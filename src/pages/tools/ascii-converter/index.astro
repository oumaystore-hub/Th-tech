---
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const canonical = `https://oumaystore-hub.github.io${base}/tools/ascii-converter/`;
---

<!doctype html>
<html lang="ar" dir="rtl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>ASCII Converter | محول ASCII | Th.Tech</title>

    <meta
      name="description"
      content="محول ASCII احترافي لتحويل النص إلى رموز ASCII والعكس مع دعم Decimal وHex وBinary."
    />

    <link rel="canonical" href={canonical} />

    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />

    <link
      href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;500;600;700;800&display=swap"
      rel="stylesheet"
    />

    <style>
      * {
        box-sizing: border-box;
      }

      html {
        scroll-behavior: smooth;
      }

      body {
        margin: 0;
        font-family: "Cairo", sans-serif;
        transition: background-color 0.25s ease, color 0.25s ease;
      }

      .ascii-page {
        --bg: #07111f;
        --card: #0d1b2d;
        --card-2: #101f33;
        --border: rgba(96, 165, 250, 0.18);
        --text: #f1f5f9;
        --muted: #94a3b8;
        --primary: #38bdf8;
        --primary-2: #2563eb;
        --input: #081522;
        --output: #071522;
        --shadow: 0 20px 50px rgba(0, 0, 0, 0.25);

        min-height: calc(100vh - 100px);
        padding: 45px 18px 70px;
        background: var(--bg);
        color: var(--text);
        transition:
          background 0.25s ease,
          color 0.25s ease;
      }

      .ascii-page.force-light {
        --bg: #f5f9ff;
        --card: #ffffff;
        --card-2: #f8fbff;
        --border: rgba(37, 99, 235, 0.15);
        --text: #0f172a;
        --muted: #64748b;
        --primary: #0284c7;
        --primary-2: #2563eb;
        --input: #f8fafc;
        --output: #f1f7ff;
        --shadow: 0 15px 40px rgba(15, 23, 42, 0.08);
      }

      .ascii-page.force-dark {
        --bg: #07111f;
        --card: #0d1b2d;
        --card-2: #101f33;
        --border: rgba(96, 165, 250, 0.18);
        --text: #f1f5f9;
        --muted: #94a3b8;
        --primary: #38bdf8;
        --primary-2: #2563eb;
        --input: #081522;
        --output: #071522;
        --shadow: 0 20px 50px rgba(0, 0, 0, 0.25);
      }

      :global(body.ascii-dark-theme) {
        background: #07111f !important;
        color: #f1f5f9;
      }

      :global(body.ascii-light-theme) {
        background: #f5f9ff !important;
        color: #0f172a;
      }

      .tool-container {
        max-width: 1180px;
        margin: 0 auto;
      }

      .tool-heading {
        text-align: center;
        margin-bottom: 32px;
      }

      .tool-icon {
        width: 72px;
        height: 72px;
        margin: 0 auto 18px;
        border-radius: 20px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, #0ea5e9, #2563eb);
        color: white;
        font-size: 30px;
        box-shadow: 0 15px 35px rgba(37, 99, 235, 0.3);
      }

      .tool-heading h1 {
        margin: 0 0 10px;
        font-size: clamp(28px, 5vw, 42px);
        font-weight: 800;
        background: linear-gradient(90deg, #38bdf8, #60a5fa);
        -webkit-background-clip: text;
        background-clip: text;
        color: transparent;
      }

      .tool-heading p {
        max-width: 700px;
        margin: 0 auto;
        color: var(--muted);
        font-size: 15px;
        line-height: 1.9;
      }

      .tool-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 22px;
        align-items: start;
      }

      .card {
        background: var(--card);
        border: 1px solid var(--border);
        border-radius: 20px;
        padding: 22px;
        box-shadow: var(--shadow);
        transition:
          background 0.25s ease,
          border-color 0.25s ease;
      }

      .card-title {
        display: flex;
        align-items: center;
        gap: 10px;
        margin-bottom: 18px;
        font-size: 18px;
        font-weight: 800;
      }

      .card-title i {
        color: var(--primary);
      }

      .mode-buttons {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 10px;
        margin-bottom: 18px;
      }

      .mode-btn {
        border: 1px solid var(--border);
        background: var(--card-2);
        color: var(--muted);
        border-radius: 12px;
        padding: 12px 10px;
        cursor: pointer;
        font-family: inherit;
        font-size: 14px;
        font-weight: 700;
        transition: 0.2s ease;
      }

      .mode-btn:hover {
        border-color: var(--primary);
        color: var(--text);
      }

      .mode-btn.active {
        background: linear-gradient(135deg, #0284c7, #2563eb);
        border-color: transparent;
        color: white;
        box-shadow: 0 8px 20px rgba(37, 99, 235, 0.25);
      }

      .field-label {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 10px;
        margin-bottom: 9px;
        color: var(--text);
        font-size: 14px;
        font-weight: 700;
      }

      .counter {
        color: var(--muted);
        font-size: 12px;
        font-weight: 500;
      }

      textarea {
        width: 100%;
        min-height: 265px;
        resize: vertical;
        border: 1px solid var(--border);
        border-radius: 14px;
        outline: none;
        padding: 16px;
        background: var(--input);
        color: var(--text);
        font-family: "Cairo", sans-serif;
        font-size: 15px;
        line-height: 1.8;
        transition:
          border-color 0.2s ease,
          box-shadow 0.2s ease,
          background 0.25s ease;
      }

      textarea:focus {
        border-color: var(--primary);
        box-shadow: 0 0 0 3px rgba(56, 189, 248, 0.1);
      }

      textarea.ltr {
        direction: ltr;
        text-align: left;
        font-family: "Courier New", monospace;
      }

      .action-row {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 9px;
        margin-top: 14px;
      }

      .btn {
        min-height: 44px;
        border: 0;
        border-radius: 11px;
        padding: 10px 12px;
        cursor: pointer;
        font-family: inherit;
        font-size: 13px;
        font-weight: 700;
        transition:
          transform 0.15s ease,
          opacity 0.2s ease,
          background 0.2s ease;
      }

      .btn:hover {
        transform: translateY(-1px);
      }

      .btn:active {
        transform: translateY(0);
      }

      .btn-primary {
        color: white;
        background: linear-gradient(135deg, #0284c7, #2563eb);
      }

      .btn-secondary {
        background: var(--card-2);
        border: 1px solid var(--border);
        color: var(--text);
      }

      .btn-danger {
        background: rgba(239, 68, 68, 0.1);
        border: 1px solid rgba(239, 68, 68, 0.2);
        color: #f87171;
      }

      .status {
        min-height: 24px;
        margin-top: 13px;
        font-size: 13px;
        font-weight: 700;
      }

      .status.success {
        color: #22c55e;
      }

      .status.error {
        color: #ef4444;
      }

      .output-box {
        width: 100%;
        min-height: 145px;
        max-height: 280px;
        overflow: auto;
        padding: 16px;
        border-radius: 14px;
        border: 1px solid var(--border);
        background: var(--output);
        color: var(--text);
        direction: ltr;
        text-align: left;
        white-space: pre-wrap;
        word-break: break-word;
        font-family: "Courier New", monospace;
        font-size: 14px;
        line-height: 1.7;
      }

      .output-empty {
        color: var(--muted);
        font-family: "Cairo", sans-serif;
        text-align: center;
        direction: rtl;
        padding-top: 25px;
      }

      .formats {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 10px;
        margin-top: 18px;
      }

      .format-box {
        min-width: 0;
        padding: 13px;
        border-radius: 13px;
        background: var(--card-2);
        border: 1px solid var(--border);
      }

      .format-name {
        color: var(--muted);
        font-size: 11px;
        font-weight: 700;
        margin-bottom: 7px;
      }

      .format-value {
        direction: ltr;
        text-align: left;
        overflow: auto;
        white-space: nowrap;
        font-family: "Courier New", monospace;
        font-size: 12px;
        color: var(--text);
      }

      .info-card {
        margin-top: 22px;
      }

      .info-list {
        display: grid;
        gap: 10px;
        margin: 0;
        padding: 0;
        list-style: none;
      }

      .info-list li {
        display: flex;
        align-items: flex-start;
        gap: 10px;
        color: var(--muted);
        font-size: 13px;
        line-height: 1.8;
      }

      .info-list i {
        color: var(--primary);
        margin-top: 5px;
      }

      .keyboard {
        display: inline-flex;
        align-items: center;
        padding: 2px 7px;
        border-radius: 6px;
        background: var(--card-2);
        border: 1px solid var(--border);
        color: var(--text);
        direction: ltr;
        font-family: monospace;
        font-size: 11px;
      }

      @media (max-width: 850px) {
        .ascii-page {
          padding: 32px 14px 55px;
        }

        .tool-grid {
          grid-template-columns: 1fr;
        }

        .action-row {
          grid-template-columns: repeat(2, 1fr);
        }
      }

      @media (max-width: 560px) {
        .ascii-page {
          padding: 26px 10px 45px;
        }

        .card {
          padding: 16px;
          border-radius: 16px;
        }

        .tool-icon {
          width: 62px;
          height: 62px;
          font-size: 25px;
          border-radius: 17px;
        }

        .tool-heading {
          margin-bottom: 24px;
        }

        .tool-heading h1 {
          font-size: 29px;
        }

        textarea {
          min-height: 220px;
          font-size: 14px;
        }

        .formats {
          grid-template-columns: 1fr;
        }

        .action-row {
          grid-template-columns: 1fr 1fr;
        }
      }

      @media (max-width: 380px) {
        .mode-buttons {
          grid-template-columns: 1fr;
        }

        .action-row {
          grid-template-columns: 1fr;
        }
      }
    </style>
  </head>

  <body>
    <Header />

    <main class="ascii-page force-dark" id="asciiPage">
<!-- TH-BREADCRUMBS-START -->
<nav class="tool-breadcrumbs" aria-label="مسار التنقل">
  <a href="https://oumaystore-hub.github.io/Th-tech/">الرئيسية</a>
  <span aria-hidden="true">←</span>

  <a href="https://oumaystore-hub.github.io/Th-tech/tools/">الأدوات</a>
  <span aria-hidden="true">←</span>

  <a href="https://oumaystore-hub.github.io/Th-tech/tools/?category=tools">
    أدوات تقنية
  </a>

  <span aria-hidden="true">←</span>

  <span aria-current="page">Ascii Converter</span>
</nav>

<style>
.tool-breadcrumbs {
  width: min(100% - 32px, 1000px);
  margin: 0 auto 18px;
  padding: 10px 14px;

  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 7px;

  font-size: 13px;
  line-height: 1.7;

  color: #64748b;
  background: #ffffff;
  border: 1px solid #e2e8f0;
  border-radius: 10px;

  box-sizing: border-box;
}

.tool-breadcrumbs a {
  color: #2563eb;
  text-decoration: none;
  transition: color 0.2s ease;
}

.tool-breadcrumbs a:hover {
  text-decoration: underline;
}

.tool-breadcrumbs span[aria-hidden="true"] {
  color: #94a3b8;
}

.tool-breadcrumbs span[aria-current="page"] {
  color: #334155;
  font-weight: 600;
}

html.dark-mode .tool-breadcrumbs {
  color: #94a3b8;
  background: #111827;
  border-color: #263449;
}

html.dark-mode .tool-breadcrumbs a {
  color: #60a5fa;
}

html.dark-mode .tool-breadcrumbs span[aria-hidden="true"] {
  color: #64748b;
}

html.dark-mode .tool-breadcrumbs span[aria-current="page"] {
  color: #e2e8f0;
}

@media (max-width: 520px) {
  .tool-breadcrumbs {
    width: calc(100% - 20px);
    margin-bottom: 14px;
    padding: 8px 10px;
    font-size: 12px;
    gap: 5px;
  }
}
</style>
<!-- TH-BREADCRUMBS-END -->

      <div class="tool-container">

        <section class="tool-heading">
          <div class="tool-icon">
            <i class="fa-solid fa-code"></i>
          </div>

          <h1>محول ASCII</h1>

          <p>
            حوّل النص إلى رموز ASCII والعكس بسهولة، مع عرض القيم العشرية
            والسداسية عشرية والثنائية.
          </p>
        </section>

        <section class="tool-grid">

          <!-- Input -->
          <div class="card">
            <div class="card-title">
              <i class="fa-solid fa-arrow-right-arrow-left"></i>
              <span>المحول</span>
            </div>

            <div class="mode-buttons">
              <button
                type="button"
                class="mode-btn active"
                id="textMode"
              >
                <i class="fa-solid fa-font"></i>
                النص → ASCII
              </button>

              <button
                type="button"
                class="mode-btn"
                id="asciiMode"
              >
                <i class="fa-solid fa-hashtag"></i>
                ASCII → النص
              </button>
            </div>

            <label class="field-label" for="inputText">
              <span id="inputLabel">النص</span>
              <span class="counter" id="counter">0 حرف</span>
            </label>

            <textarea
              id="inputText"
              placeholder="اكتب النص هنا..."
              autocomplete="off"
              spellcheck="false"
            ></textarea>

            <div class="action-row">
              <button type="button" class="btn btn-primary" id="convertBtn">
                <i class="fa-solid fa-bolt"></i>
                تحويل
              </button>

              <button type="button" class="btn btn-secondary" id="pasteBtn">
                <i class="fa-solid fa-paste"></i>
                لصق
              </button>

              <button type="button" class="btn btn-secondary" id="swapBtn">
                <i class="fa-solid fa-right-left"></i>
                تبديل
              </button>

              <button type="button" class="btn btn-danger" id="clearBtn">
                <i class="fa-solid fa-trash"></i>
                مسح
              </button>
            </div>

            <div class="status" id="status"></div>
          </div>

          <!-- Output -->
          <div class="card">
            <div class="card-title">
              <i class="fa-solid fa-terminal"></i>
              <span>النتيجة</span>
            </div>

            <div class="output-box" id="output">
              <div class="output-empty">
                ستظهر النتيجة هنا بعد التحويل
              </div>
            </div>

            <div class="action-row" style="margin-top: 14px;">
              <button type="button" class="btn btn-primary" id="copyBtn">
                <i class="fa-solid fa-copy"></i>
                نسخ النتيجة
              </button>
            </div>

            <div class="formats">
              <div class="format-box">
                <div class="format-name">DECIMAL</div>
                <div class="format-value" id="decimalOutput">—</div>
              </div>

              <div class="format-box">
                <div class="format-name">HEX</div>
                <div class="format-value" id="hexOutput">—</div>
              </div>

              <div class="format-box">
                <div class="format-name">BINARY</div>
                <div class="format-value" id="binaryOutput">—</div>
              </div>
            </div>
          </div>

        </section>

        <section class="card info-card">
          <div class="card-title">
            <i class="fa-solid fa-circle-info"></i>
            <span>معلومات عن ASCII</span>
          </div>

          <ul class="info-list">
            <li>
              <i class="fa-solid fa-check"></i>
              <span>
                معيار ASCII الأساسي يستخدم القيم من 0 إلى 127.
              </span>
            </li>

            <li>
              <i class="fa-solid fa-check"></i>
              <span>
                مثال: الحرف <strong>A</strong> يساوي
                <strong>65</strong> بالعشري و
                <strong>41</strong> بالـ Hex.
              </span>
            </li>

            <li>
              <i class="fa-solid fa-check"></i>
              <span>
                يمكنك في وضع ASCII إدخال الأرقام مفصولة بمسافات أو فواصل.
              </span>
            </li>

            <li>
              <i class="fa-solid fa-keyboard"></i>
              <span>
                للتحويل بسرعة استخدم
                <span class="keyboard">Ctrl + Enter</span>
                أو
                <span class="keyboard">⌘ + Enter</span>.
              </span>
            </li>
          </ul>
        </section>

      </div>
    









<!-- TH-RELATED-TOOLS-START -->
<section
  class="related-tools"
  aria-labelledby="related-tools-title"
>
  <div class="related-tools-header">
    <div>
      <span class="related-tools-label">
        استكشف المزيد
      </span>

      <h2 id="related-tools-title">
        أدوات ذات صلة
      </h2>

      <p>
        أدوات أخرى قد تساعدك في إكمال مهمتك.
      </p>
    </div>
  </div>

  <div class="related-tools-grid">
    
        <a
          class="related-tool-card"
          href={`${base}/tools/ascii-table/`}
        >
          <span class="related-tool-icon">
            
    <svg viewBox="0 0 24 24" aria-hidden="true">
      <path d="M5 4h14v16H5z"/>
      <path d="M8 8h8M8 12h8M8 16h5"/>
    </svg>
          </span>

          <span class="related-tool-content">
            <strong>Ascii Table</strong>
            <small>أداة مرتبطة</small>
          </span>

          <span class="related-tool-arrow" aria-hidden="true">
            ←
          </span>
        </a>
        <a
          class="related-tool-card"
          href={`${base}/tools/chmod-converter/`}
        >
          <span class="related-tool-icon">
            
    <svg viewBox="0 0 24 24" aria-hidden="true">
      <path d="M5 4h14v16H5z"/>
      <path d="M8 8h8M8 12h8M8 16h5"/>
    </svg>
          </span>

          <span class="related-tool-content">
            <strong>Chmod Converter</strong>
            <small>أداة مرتبطة</small>
          </span>

          <span class="related-tool-arrow" aria-hidden="true">
            ←
          </span>
        </a>
        <a
          class="related-tool-card"
          href={`${base}/tools/color-converter/`}
        >
          <span class="related-tool-icon">
            
      <svg viewBox="0 0 24 24" aria-hidden="true">
        <circle cx="12" cy="12" r="8"/>
        <circle cx="9" cy="9" r="1.5"/>
        <circle cx="15" cy="9" r="1.5"/>
        <circle cx="9" cy="15" r="1.5"/>
        <path d="M14 15h.01"/>
      </svg>
          </span>

          <span class="related-tool-content">
            <strong>Color Converter</strong>
            <small>أداة مرتبطة</small>
          </span>

          <span class="related-tool-arrow" aria-hidden="true">
            ←
          </span>
        </a>
        <a
          class="related-tool-card"
          href={`${base}/tools/number-base-converter/`}
        >
          <span class="related-tool-icon">
            
    <svg viewBox="0 0 24 24" aria-hidden="true">
      <path d="M5 4h14v16H5z"/>
      <path d="M8 8h8M8 12h8M8 16h5"/>
    </svg>
          </span>

          <span class="related-tool-content">
            <strong>Number Base Converter</strong>
            <small>أداة مرتبطة</small>
          </span>

          <span class="related-tool-arrow" aria-hidden="true">
            ←
          </span>
        </a>
        <a
          class="related-tool-card"
          href={`${base}/tools/text-case-converter/`}
        >
          <span class="related-tool-icon">
            
      <svg viewBox="0 0 24 24" aria-hidden="true">
        <path d="M5 5h14M12 5v14M8 19h8"/>
        <path d="M8 9h8"/>
      </svg>
          </span>

          <span class="related-tool-content">
            <strong>Text Case Converter</strong>
            <small>أداة مرتبطة</small>
          </span>

          <span class="related-tool-arrow" aria-hidden="true">
            ←
          </span>
        </a>
        <a
          class="related-tool-card"
          href={`${base}/tools/time-converter/`}
        >
          <span class="related-tool-icon">
            
      <svg viewBox="0 0 24 24" aria-hidden="true">
        <circle cx="12" cy="12" r="8"/>
        <path d="M12 7v5l3 2"/>
      </svg>
          </span>

          <span class="related-tool-content">
            <strong>Time Converter</strong>
            <small>أداة مرتبطة</small>
          </span>

          <span class="related-tool-arrow" aria-hidden="true">
            ←
          </span>
        </a>
  </div>
</section>

<style>
  /* =========================================
     Related Tools
     الوضع الافتراضي = فاتح
     ========================================= */

  .related-tools {
    width: min(1000px, 100%);
    margin: 40px auto 25px;
    padding: 22px;

    border-radius: 18px;

    background: #ffffff !important;
    color: #0f172a !important;

    border: 1px solid #e2e8f0 !important;

    box-shadow:
      0 8px 24px rgba(15, 23, 42, 0.06) !important;

    transition:
      background 0.25s ease,
      color 0.25s ease,
      border-color 0.25s ease;
  }

  .related-tools-header {
    margin-bottom: 17px;
  }

  .related-tools-label {
    display: inline-block;

    margin-bottom: 5px;

    color: #0284c7 !important;

    font-size: 0.78rem;
    font-weight: 800;
  }

  .related-tools h2 {
    margin: 0 0 5px;

    color: #0f172a !important;

    font-size: 1.3rem;
    line-height: 1.4;
    font-weight: 800;
  }

  .related-tools p {
    margin: 0;

    color: #64748b !important;

    font-size: 0.85rem;
  }

  /* =========================================
     Cards
     ========================================= */

  .related-tools-grid {
    display: grid;

    grid-template-columns:
      repeat(3, minmax(0, 1fr));

    gap: 10px;
  }

  .related-tool-card {
    display: flex;
    align-items: center;

    gap: 9px;

    min-height: 58px;

    padding: 9px 10px;

    border-radius: 12px;

    text-decoration: none !important;

    color: #0f172a !important;

    background: #f8fafc !important;

    border: 1px solid #e2e8f0 !important;

    box-shadow:
      0 2px 8px rgba(15, 23, 42, 0.035) !important;

    transition:
      transform 0.18s ease,
      border-color 0.18s ease,
      background 0.18s ease,
      box-shadow 0.18s ease;
  }

  .related-tool-card:hover {
    transform: translateY(-2px);

    color: #0f172a !important;

    background: #f0f9ff !important;

    border-color: #38bdf8 !important;

    box-shadow:
      0 6px 14px rgba(14, 165, 233, 0.10) !important;
  }

  /* =========================================
     SVG icon
     ========================================= */

  .related-tool-icon {
    display: grid;
    place-items: center;

    width: 34px;
    height: 34px;

    flex: 0 0 34px;

    border-radius: 9px;

    color: #0284c7 !important;

    background: rgba(14, 165, 233, 0.10) !important;
  }

  .related-tool-icon svg {
    width: 18px;
    height: 18px;

    fill: none;
    stroke: currentColor;

    stroke-width: 1.8;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .related-tool-content {
    display: flex;
    flex-direction: column;

    justify-content: center;

    gap: 2px;

    min-width: 0;
    flex: 1;
  }

  .related-tool-content strong {
    display: block;

    color: #0f172a !important;

    font-size: 0.84rem;
    line-height: 1.25;
    font-weight: 750;

    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .related-tool-content small {
    color: #64748b !important;

    font-size: 0.68rem;
    line-height: 1.2;
  }

  .related-tool-arrow {
    display: flex;
    align-items: center;
    justify-content: center;

    width: 22px;
    height: 22px;

    flex: 0 0 22px;

    color: #94a3b8 !important;

    font-size: 0.85rem;

    transition:
      transform 0.18s ease,
      color 0.18s ease;
  }

  .related-tool-card:hover .related-tool-arrow {
    color: #0284c7 !important;

    transform: translateX(-2px);
  }

  /* =========================================
     الوضع الليلي الحقيقي في المشروع
     يعتمد على class وليس data-theme
     ========================================= */

  html.dark-mode .related-tools,
  body.dark-mode .related-tools,
  html.dark .related-tools,
  body.dark .related-tools {
    background: #111827 !important;
    color: #f8fafc !important;

    border-color: rgba(255, 255, 255, 0.08) !important;

    box-shadow:
      0 10px 30px rgba(0, 0, 0, 0.20) !important;
  }

  html.dark-mode .related-tools h2,
  body.dark-mode .related-tools h2,
  html.dark .related-tools h2,
  body.dark .related-tools h2 {
    color: #f8fafc !important;
  }

  html.dark-mode .related-tools p,
  body.dark-mode .related-tools p,
  html.dark .related-tools p,
  body.dark .related-tools p {
    color: #cbd5e1 !important;
  }

  html.dark-mode .related-tool-card,
  body.dark-mode .related-tool-card,
  html.dark .related-tool-card,
  body.dark .related-tool-card {
    color: #f8fafc !important;

    background: #1e293b !important;

    border-color: rgba(255, 255, 255, 0.08) !important;

    box-shadow: none !important;
  }

  html.dark-mode .related-tool-card:hover,
  body.dark-mode .related-tool-card:hover,
  html.dark .related-tool-card:hover,
  body.dark .related-tool-card:hover {
    color: #ffffff !important;

    background: #263449 !important;

    border-color: #38bdf8 !important;
  }

  html.dark-mode .related-tool-content strong,
  body.dark-mode .related-tool-content strong,
  html.dark .related-tool-content strong,
  body.dark .related-tool-content strong {
    color: #f8fafc !important;
  }

  html.dark-mode .related-tool-content small,
  body.dark-mode .related-tool-content small,
  html.dark .related-tool-content small,
  body.dark .related-tool-content small {
    color: #94a3b8 !important;
  }

  html.dark-mode .related-tool-icon,
  body.dark-mode .related-tool-icon,
  html.dark .related-tool-icon,
  body.dark .related-tool-icon {
    color: #38bdf8 !important;

    background: rgba(56, 189, 248, 0.12) !important;
  }

  html.dark-mode .related-tool-arrow,
  body.dark-mode .related-tool-arrow,
  html.dark .related-tool-arrow,
  body.dark .related-tool-arrow {
    color: #64748b !important;
  }

  /* =========================================
     الوضع النهاري
     نتأكد أنه يتغلب على أي CSS عام
     ========================================= */

  html:not(.dark-mode) .related-tools,
  body:not(.dark-mode) .related-tools,
  html:not(.dark) .related-tools {
    background: #ffffff !important;

    color: #0f172a !important;

    border-color: #e2e8f0 !important;

    box-shadow:
      0 8px 24px rgba(15, 23, 42, 0.06) !important;
  }

  html:not(.dark-mode) .related-tool-card,
  body:not(.dark-mode) .related-tool-card,
  html:not(.dark) .related-tool-card {
    background: #f8fafc !important;

    color: #0f172a !important;

    border-color: #e2e8f0 !important;

    box-shadow:
      0 2px 8px rgba(15, 23, 42, 0.035) !important;
  }

  html:not(.dark-mode) .related-tool-content strong,
  body:not(.dark-mode) .related-tool-content strong,
  html:not(.dark) .related-tool-content strong {
    color: #0f172a !important;
  }

  html:not(.dark-mode) .related-tool-content small,
  body:not(.dark-mode) .related-tool-content small,
  html:not(.dark) .related-tool-content small {
    color: #64748b !important;
  }

  /* =========================================
     Responsive
     ========================================= */

  @media (max-width: 800px) {
    .related-tools {
      padding: 18px;
      margin-top: 30px;
    }

    .related-tools-grid {
      grid-template-columns:
        repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 520px) {
    .related-tools {
      padding: 16px;
    }

    .related-tools-grid {
      grid-template-columns: 1fr;
    }

    .related-tool-card {
      min-height: 54px;
      padding: 8px 9px;
    }

    .related-tool-icon {
      width: 32px;
      height: 32px;
      flex-basis: 32px;
    }

    .related-tool-icon svg {
      width: 17px;
      height: 17px;
    }
  }
</style>
<!-- TH-RELATED-TOOLS-END -->

</main>

    <Footer />

    <script is:inline>
      (() => {
        function getThemeState() {
          const html = document.documentElement;
          const body = document.body;

          const stored = localStorage.getItem("theme");

          if (stored === "light") return "light";
          if (stored === "dark") return "dark";

          if (
            html.dataset.theme === "light" ||
            body.dataset.theme === "light" ||
            html.classList.contains("light") ||
            body.classList.contains("light")
          ) {
            return "light";
          }

          if (
            html.dataset.theme === "dark" ||
            body.dataset.theme === "dark" ||
            html.classList.contains("dark") ||
            body.classList.contains("dark")
          ) {
            return "dark";
          }

          return "dark";
        }

        function syncAsciiTheme() {
          const page = document.getElementById("asciiPage");

          if (!page) return;

          const theme = getThemeState();
          const light = theme === "light";

          /*
           * مهم:
           * نستخدم toggle مع القيمة الثانية حتى لا ندخل
           * في حلقة لا نهائية مع MutationObserver.
           */
          page.classList.toggle("force-light", light);
          page.classList.toggle("force-dark", !light);

          document.body.classList.toggle("ascii-light-theme", light);
          document.body.classList.toggle("ascii-dark-theme", !light);
        }

        function watchTheme() {
          syncAsciiTheme();

          window.addEventListener("storage", (event) => {
            if (event.key === "theme") {
              syncAsciiTheme();
            }
          });

          const observer = new MutationObserver(() => {
            syncAsciiTheme();
          });

          observer.observe(document.documentElement, {
            attributes: true,
            attributeFilter: ["class", "data-theme"]
          });

          observer.observe(document.body, {
            attributes: true,
            attributeFilter: ["class", "data-theme"]
          });
        }

        function startAsciiTool() {
          const input = document.getElementById("inputText");
          const output = document.getElementById("output");

          const textMode = document.getElementById("textMode");
          const asciiMode = document.getElementById("asciiMode");

          const convertBtn = document.getElementById("convertBtn");
          const pasteBtn = document.getElementById("pasteBtn");
          const swapBtn = document.getElementById("swapBtn");
          const clearBtn = document.getElementById("clearBtn");
          const copyBtn = document.getElementById("copyBtn");

          const inputLabel = document.getElementById("inputLabel");
          const counter = document.getElementById("counter");
          const status = document.getElementById("status");

          const decimalOutput = document.getElementById("decimalOutput");
          const hexOutput = document.getElementById("hexOutput");
          const binaryOutput = document.getElementById("binaryOutput");

          if (
            !input ||
            !output ||
            !textMode ||
            !asciiMode ||
            !convertBtn
          ) {
            return;
          }

          let mode = "text";

          function setStatus(message, type = "") {
            status.textContent = message;
            status.className = "status";

            if (type) {
              status.classList.add(type);
            }
          }

          function updateCounter() {
            const length = input.value.length;

            if (mode === "text") {
              counter.textContent = `${length} حرف`;
            } else {
              const count = input.value
                .trim()
                .replace(/[,;]+/g, " ")
                .split(/\s+/)
                .filter(Boolean).length;

              counter.textContent = `${count} قيمة`;
            }
          }

          function updateMode() {
            const isText = mode === "text";

            textMode.classList.toggle("active", isText);
            asciiMode.classList.toggle("active", !isText);

            if (isText) {
              inputLabel.textContent = "النص";
              input.placeholder = "اكتب النص هنا...";
              input.classList.remove("ltr");
            } else {
              inputLabel.textContent = "رموز ASCII";
              input.placeholder = "مثال: 72 101 108 108 111";
              input.classList.add("ltr");
            }

            updateCounter();
            setStatus("");
          }

          function showResult(text, codes) {
            output.textContent = text || "";

            if (!text) {
              output.innerHTML =
                '<div class="output-empty">ستظهر النتيجة هنا بعد التحويل</div>';
            }

            const decimal = codes.join(" ");

            const hex = codes
              .map((code) => code.toString(16).toUpperCase().padStart(2, "0"))
              .join(" ");

            const binary = codes
              .map((code) => code.toString(2).padStart(8, "0"))
              .join(" ");

            decimalOutput.textContent = decimal || "—";
            hexOutput.textContent = hex || "—";
            binaryOutput.textContent = binary || "—";
          }

          function convertTextToAscii() {
            const value = input.value;

            if (!value) {
              showResult("", []);
              setStatus("اكتب نصًا أولًا.", "error");
              return;
            }

            const codes = [];

            for (let i = 0; i < value.length; i++) {
              const code = value.charCodeAt(i);

              if (code > 127) {
                showResult("", []);
                setStatus(
                  "ASCII الأساسي يدعم الأحرف والقيم من 0 إلى 127 فقط.",
                  "error"
                );
                return;
              }

              codes.push(code);
            }

            const result = codes.join(" ");

            showResult(result, codes);
            setStatus("تم تحويل النص بنجاح.", "success");
          }

          function parseAscii(value) {
            const clean = value
              .replace(/[,;]+/g, " ")
              .replace(/\s+/g, " ")
              .trim();

            if (!clean) {
              return [];
            }

            const parts = clean.split(" ");
            const codes = [];

            for (let i = 0; i < parts.length; i++) {
              const part = parts[i];

              if (!/^\d+$/.test(part)) {
                throw new Error(
                  `القيمة "${part}" ليست رقم ASCII صالحًا.`
                );
              }

              const number = Number(part);

              if (!Number.isInteger(number) || number < 0 || number > 127) {
                throw new Error(
                  `القيمة "${part}" يجب أن تكون بين 0 و127.`
                );
              }

              codes.push(number);
            }

            return codes;
          }

          function convertAsciiToText() {
            const value = input.value.trim();

            if (!value) {
              showResult("", []);
              setStatus("أدخل رموز ASCII أولًا.", "error");
              return;
            }

            try {
              const codes = parseAscii(value);

              let result = "";

              for (let i = 0; i < codes.length; i++) {
                result += String.fromCharCode(codes[i]);
              }

              showResult(result, codes);
              setStatus("تم تحويل ASCII بنجاح.", "success");
            } catch (error) {
              showResult("", []);
              setStatus(error.message, "error");
            }
          }

          function convert() {
            if (mode === "text") {
              convertTextToAscii();
            } else {
              convertAsciiToText();
            }
          }

          textMode.addEventListener("click", () => {
            if (mode === "text") return;

            mode = "text";
            input.value = "";
            showResult("", []);
            updateMode();
            input.focus();
          });

          asciiMode.addEventListener("click", () => {
            if (mode === "ascii") return;

            mode = "ascii";
            input.value = "";
            showResult("", []);
            updateMode();
            input.focus();
          });

          convertBtn.addEventListener("click", convert);

          input.addEventListener("input", updateCounter);

          input.addEventListener("keydown", (event) => {
            if ((event.ctrlKey || event.metaKey) && event.key === "Enter") {
              event.preventDefault();
              convert();
            }
          });

          clearBtn.addEventListener("click", () => {
            input.value = "";

            showResult("", []);
            setStatus("");
            updateCounter();

            input.focus();
          });

          swapBtn.addEventListener("click", () => {
            mode = mode === "text" ? "ascii" : "text";

            input.value = "";

            showResult("", []);
            updateMode();

            input.focus();
          });

          pasteBtn.addEventListener("click", async () => {
            try {
              const text = await navigator.clipboard.readText();

              input.value = text;
              updateCounter();

              setStatus("تم لصق النص.", "success");
              input.focus();
            } catch (error) {
              setStatus(
                "تعذر الوصول إلى الحافظة. استخدم اللصق من لوحة المفاتيح.",
                "error"
              );
            }
          });

          copyBtn.addEventListener("click", async () => {
            const text = output.textContent.trim();

            if (
              !text ||
              text === "ستظهر النتيجة هنا بعد التحويل"
            ) {
              setStatus("لا توجد نتيجة لنسخها.", "error");
              return;
            }

            try {
              await navigator.clipboard.writeText(text);

              setStatus("تم نسخ النتيجة.", "success");
            } catch (error) {
              setStatus(
                "تعذر النسخ تلقائيًا. حدد النتيجة وانسخها يدويًا.",
                "error"
              );
            }
          });

          updateMode();
          showResult("", []);
        }

        function init() {
          watchTheme();
          startAsciiTool();
        }

        if (document.readyState === "loading") {
          document.addEventListener("DOMContentLoaded", init, {
            once: true
          });
        } else {
          init();
        }
      })();
    </script>
  </body>
</html>
