---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---
<html lang="ar" dir="rtl" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Th.Tech | المنصة التقنية المتقدمة</title>
  <meta name="description" content="منصة عربية متخصصة في الذكاء الاصطناعي، البرمجة، والأمن السيبراني" />
  <link rel="icon" type="image/png" href="/Th-tech/logo1.png" />
  <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700;800;900&display=swap" rel="stylesheet" />
</head>
<body>
  <Header />

  <main>
    <!-- Hero Section -->
    <section class="hero">
      <div class="hero-bg">
        <img src="https://images.unsplash.com/photo-1677442136019-21780ecad995?w=1920&h=1080&fit=crop" alt="AI Technology" loading="eager" />
        <div class="overlay"></div>
      </div>
      <div class="container">
        <div class="hero-content">
          <span class="badge">🚀 منصة المطورين العربية المتقدمة</span>
          <h1>
            <span class="highlight">ابنِ مستقبلك الرقمي</span><br />
            مع نخبة الخبراء
          </h1>
          <p class="hero-desc">
            شروحات عملية متعمقة في الذكاء الاصطناعي، هندسة البرمجيات، والأمن السيبراني.<br />
            محتوى تقني احترافي يُحدث أسبوعياً ليواكب أحدث التقنيات العالمية.
          </p>
          <div class="hero-actions">
            <a href="/Th-tech/guides/" class="btn btn-primary">
              <span>📚 استكشف الأدلة</span>
            </a>
            <a href="/Th-tech/playground/" class="btn btn-outline">
              <span>💻 المختبر التفاعلي</span>
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- Stats Bar -->
    <section class="stats-bar">
      <div class="container">
        <div class="stats-grid">
          <div class="stat-item">
            <span class="stat-icon"></span>
            <span class="stat-number" data-target="150" data-suffix="+">0</span>
            <span class="stat-label">دليل تقني شامل</span>
          </div>
          <div class="stat-divider"></div>
          <div class="stat-item">
            <span class="stat-icon"></span>
            <span class="stat-number" data-target="50" data-suffix="K+">0</span>
            <span class="stat-label">قارئ شهرياً</span>
          </div>
          <div class="stat-divider"></div>
          <div class="stat-item">
            <span class="stat-icon"></span>
            <span class="stat-number" data-target="100" data-suffix="%">0</span>
            <span class="stat-label">محتوى مجاني</span>
          </div>
        </div>
      </div>
    </section>

    <!-- Features Grid -->
    <section class="features">
      <div class="container">
        <div class="section-header">
          <h2>المسارات التقنية المتخصصة</h2>
          <p>اختر مجال تخصصك وابدأ رحلتك الاحترافية</p>
        </div>
        
        <div class="features-grid">
          <a href="/Th-tech/ai/" class="feature-card">
            <div class="card-image">
              <img src="https://images.unsplash.com/photo-1677442136019-21780ecad995?w=600&h=350&fit=crop" alt="AI" loading="lazy" />
              <div class="card-overlay"></div>
            </div>
            <div class="card-content">
              <div class="card-icon">🤖</div>
              <h3>الذكاء الاصطناعي</h3>
              <p>تعلم الآلة، النماذج اللغوية الكبيرة (LLMs)، والشبكات العصبية العميقة</p>
              <ul class="card-list">
                <li>✓ تعلم الآلة المتقدم</li>
                <li>✓ معالجة اللغة الطبيعية</li>
                <li>✓ الرؤية الحاسوبية</li>
              </ul>
              <span class="card-link">اكتشف المسار ←</span>
            </div>
          </a>

          <a href="/Th-tech/programming/" class="feature-card">
            <div class="card-image">
              <img src="https://images.unsplash.com/photo-1555066931-4365d14bab8c?w=600&h=350&fit=crop" alt="Programming" loading="lazy" />
              <div class="card-overlay"></div>
            </div>
            <div class="card-content">
              <div class="card-icon">💻</div>
              <h3>هندسة البرمجيات</h3>
              <p>تطوير التطبيقات، الأنماط المعمارية، وأفضل ممارسات البرمجة</p>
              <ul class="card-list">
                <li>✓ الأطر الحديثة (React, Node.js)</li>
                <li>✓ الأنماط التصميمية</li>
                <li>✓ DevOps و CI/CD</li>
              </ul>
              <span class="card-link">اكتشف المسار ←</span>
            </div>
          </a>

          <a href="/Th-tech/security/" class="feature-card">
            <div class="card-image">
              <img src="https://images.unsplash.com/photo-1550751827-4bd374c3f58b?w=600&h=350&fit=crop" alt="Security" loading="lazy" />
              <div class="card-overlay"></div>
            </div>
            <div class="card-content">
              <div class="card-icon">🔒</div>
              <h3>الأمن السيبراني</h3>
              <p>حماية البنى التحتية، اختبار الاختراق، والتحليل الجنائي الرقمي</p>
              <ul class="card-list">
                <li>✓ الأمن الدفاعي والهجومي</li>
                <li>✓ تحليل الثغرات الأمنية</li>
                <li>✓ التشفير وأمن الشبكات</li>
              </ul>
              <span class="card-link">اكتشف المسار ←</span>
            </div>
          </a>

          <a href="/Th-tech/cloud/" class="feature-card">
            <div class="card-image">
              <img src="https://images.unsplash.com/photo-1451187580459-43490279c0fa?w=600&h=350&fit=crop" alt="Cloud" loading="lazy" />
              <div class="card-overlay"></div>
            </div>
            <div class="card-content">
              <div class="card-icon">☁️</div>
              <h3>الحوسبة السحابية</h3>
              <p>نشر وإدارة التطبيقات على AWS، Azure، و Google Cloud</p>
              <ul class="card-list">
                <li>✓ البنية التحتية ككود (IaC)</li>
                <li>✓ الحاويات و Kubernetes</li>
                <li>✓ Serverless Architecture</li>
              </ul>
              <span class="card-link">اكتشف المسار ←</span>
            </div>
          </a>
        </div>
      </div>
    </section>

    <!-- Why Choose Us -->
    <section class="why-us">
      <div class="container">
        <div class="why-grid">
          <div class="why-content">
            <h2>لماذا Th.Tech؟</h2>
            <p class="why-desc">منصة عربية رائدة تُقدم محتوى تقنياً معمقاً يُواكب أحدث الابتكارات العالمية مع التركيز على التطبيق العملي</p>
            
            <div class="why-features">
              <div class="why-item">
                <div class="why-icon">⚡</div>
                <div>
                  <h4>محتوى تطبيقي عملي</h4>
                  <p>شروحات مع أمثلة واقعية قابلة للتنفيذ الفوري في مشاريعك</p>
                </div>
              </div>
              
              <div class="why-item">
                <div class="why-icon">🔄</div>
                <div>
                  <h4>تحديث مستمر أسبوعي</h4>
                  <p>محتوى يُحدَّث باستمرار ليعكس أحدث التقنيات والاتجاهات</p>
                </div>
              </div>
              
              <div class="why-item">
                <div class="why-icon"></div>
                <div>
                  <h4>مسار تعلم واضح</h4>
                  <p>من الأساسيات إلى الاحتراف مع مسارات منظمة ومرتبة</p>
                </div>
              </div>
              
              <div class="why-item">
                <div class="why-icon">👨🏫</div>
                <div>
                  <h4>خبراء متخصصون</h4>
                  <p>محتوى يُعد بواسطة خبراء في المجال التقني بسنوات خبرة</p>
                </div>
              </div>
            </div>
          </div>

          <div class="why-visual">
            <div class="visual-card">
              <div class="visual-icon">📚</div>
              <div class="visual-number" data-target="150" data-suffix="+">0</div>
              <div class="visual-label">دليل شامل</div>
            </div>
            <div class="visual-card">
              <div class="visual-icon">👥</div>
              <div class="visual-number" data-target="50" data-suffix="K+">0</div>
              <div class="visual-label">قارئ شهرياً</div>
            </div>
            <div class="visual-card">
              <div class="visual-icon">🎓</div>
              <div class="visual-number" data-target="100" data-suffix="%">0</div>
              <div class="visual-label">محتوى مجاني</div>
            </div>
            <div class="visual-card">
              <div class="visual-icon">️</div>
              <div class="visual-number" data-target="24" data-suffix="/7">0</div>
              <div class="visual-label">دعم متواصل</div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Latest Articles -->
    <section class="articles">
      <div class="container">
        <div class="section-header">
          <h2>أحدث المقالات</h2>
          <p>محتوى تقني محدّث باستمرار لمواكبة التطورات</p>
        </div>
        
        <div class="articles-grid">
          <article class="article-card">
            <div class="article-image">
              <img src="https://images.unsplash.com/photo-1677442136019-21780ecad995?w=600&h=400&fit=crop" alt="AI" loading="lazy" />
              <span class="article-badge">جديد</span>
            </div>
            <div class="article-content">
              <div class="article-meta">
                <span class="tag ai">AI</span>
                <span class="read-time">5 دقائق</span>
              </div>
              <h3>دليلك الشامل لتعلم الآلة</h3>
              <p>رحلة عملية من الصفر للاحتراف في بناء النماذج الذكية</p>
            </div>
          </article>

          <article class="article-card">
            <div class="article-image">
              <img src="https://images.unsplash.com/photo-1550751827-4bd374c3f58b?w=600&h=400&fit=crop" alt="Security" loading="lazy" />
              <span class="article-badge popular">شائع</span>
            </div>
            <div class="article-content">
              <div class="article-meta">
                <span class="tag security">Security</span>
                <span class="read-time">8 دقائق</span>
              </div>
              <h3>أمن السحابة: أفضل الممارسات</h3>
              <p>استراتيجيات حماية متقدمة لتطبيقاتك على AWS و Azure</p>
            </div>
          </article>

          <article class="article-card">
            <div class="article-image">
              <img src="https://images.unsplash.com/photo-1555066931-4365d14bab8c?w=600&h=400&fit=crop" alt="Code" loading="lazy" />
              <span class="article-badge updated">محدّث</span>
            </div>
            <div class="article-content">
              <div class="article-meta">
                <span class="tag code">Code</span>
                <span class="read-time">6 دقائق</span>
              </div>
              <h3>مبادئ SOLID للمحترفين</h3>
              <p>الأنماط التصميمية التي تفصل بين الكود العادي والاحترافي</p>
            </div>
          </article>
        </div>
      </div>
    </section>

    <!-- Newsletter -->
    <section class="newsletter">
      <div class="container">
        <div class="newsletter-box">
          <div class="newsletter-icon">📬</div>
          <h2>اشترك في نشرتنا البريدية</h2>
          <p>احصل على أحدث المقالات والشروحات مباشرة في بريدك الإلكتروني</p>
          <form class="newsletter-form">
            <input type="email" placeholder="أدخل بريدك الإلكتروني" required />
            <button type="submit" class="btn btn-primary">اشترك الآن</button>
          </form>
          <p class="newsletter-note">🔒 نحمي خصوصيتك. لا رسائل مزعجة.</p>
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
      <div class="container">
        <div class="cta-box">
          <div class="cta-content">
            <h2>🚀 ابدأ رحلتك التقنية اليوم</h2>
            <p>انضم إلى آلاف المطورين والمبتكرين الذين يبنون المستقبل معنا</p>
            <div class="cta-actions">
              <a href="/Th-tech/guides/" class="btn btn-primary btn-large">تصفح الأدلة الشاملة</a>
              <a href="/Th-tech/playground/" class="btn btn-outline btn-large">جرّب المختبر التفاعلي</a>
            </div>
          </div>
        </div>
      </div>
    </section>
  </main>

  <Footer />
</body>
</html>

<style>
  :root {
    --bg: #ffffff;
    --bg-secondary: #f8fafc;
    --bg-tertiary: #f1f5f9;
    --text: #0f172a;
    --text-secondary: #475569;
    --text-muted: #64748b;
    --border: #e2e8f0;
    --primary: #2563eb;
    --primary-hover: #1d4ed8;
    --accent: #06b6d4;
    --card: #ffffff;
    --shadow: 0 4px 6px -1px rgba(0,0,0,0.1);
    --shadow-lg: 0 20px 25px -5px rgba(0,0,0,0.1);
  }

  [data-theme="dark"] {
    --bg: #0b1120;
    --bg-secondary: #0f172a;
    --bg-tertiary: #1e293b;
    --text: #f1f5f9;
    --text-secondary: #cbd5e1;
    --text-muted: #94a3b8;
    --border: #334155;
    --primary: #3b82f6;
    --primary-hover: #2563eb;
    --card: #1e293b;
    --shadow: 0 4px 6px -1px rgba(0,0,0,0.3);
    --shadow-lg: 0 20px 25px -5px rgba(0,0,0,0.5);
  }

  * { box-sizing: border-box; margin: 0; padding: 0; }
  html { scroll-behavior: smooth; }
  body { 
    background: var(--bg); 
    color: var(--text); 
    font-family: 'Cairo', sans-serif; 
    line-height: 1.6;
    transition: background-color 0.3s ease, color 0.3s ease;
  }

  .container { max-width: 1200px; margin: 0 auto; padding: 0 2rem; }
  section { padding: 5rem 0; }

  /* Hero Section */
  .hero {
    position: relative;
    min-height: 90vh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    color: white;
    overflow: hidden;
    padding: 6rem 0 4rem;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    z-index: -1;
  }

  .hero-bg img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: brightness(0.4);
  }

  .overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(37,99,235,0.4) 0%, rgba(6,182,212,0.4) 100%);
  }

  .hero-content {
    position: relative;
    z-index: 1;
    max-width: 900px;
    padding: 2rem;
  }

  .badge {
    display: inline-block;
    background: rgba(255,255,255,0.15);
    backdrop-filter: blur(10px);
    padding: 0.5rem 1.5rem;
    border-radius: 50px;
    font-size: 0.95rem;
    font-weight: 600;
    margin-bottom: 2rem;
    border: 1px solid rgba(255,255,255,0.3);
  }

  .hero h1 {
    font-size: clamp(2.5rem, 6vw, 4rem);
    font-weight: 900;
    line-height: 1.1;
    margin-bottom: 1.5rem;
  }

  .highlight {
    background: linear-gradient(135deg, #fff 0%, #60a5fa 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-desc {
    font-size: 1.25rem;
    opacity: 0.95;
    max-width: 700px;
    margin: 0 auto 2.5rem;
    line-height: 1.8;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 1rem 2.5rem;
    border-radius: 12px;
    font-weight: 700;
    font-size: 1.05rem;
    text-decoration: none;
    transition: all 0.3s;
    cursor: pointer;
    border: none;
  }

  .btn-primary {
    background: var(--primary);
    color: white;
    box-shadow: 0 4px 15px rgba(37,99,235,0.3);
  }

  .btn-primary:hover {
    background: var(--primary-hover);
    transform: translateY(-3px);
    box-shadow: 0 8px 25px rgba(37,99,235,0.4);
  }

  .btn-outline {
    background: transparent;
    border: 2px solid white;
    color: white;
  }

  .btn-outline:hover {
    background: white;
    color: var(--primary);
    transform: translateY(-3px);
  }

  .btn-large {
    padding: 1.2rem 3rem;
    font-size: 1.1rem;
  }

  /* Stats Bar */
  .stats-bar {
    background: var(--bg-secondary);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
    padding: 3rem 0;
  }

  .stats-grid {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 3rem;
  }

  .stat-item {
    text-align: center;
  }

  .stat-icon {
    font-size: 2rem;
    display: block;
    margin-bottom: 0.5rem;
  }

  .stat-number {
    display: block;
    font-size: 2.5rem;
    font-weight: 900;
    color: var(--primary);
    line-height: 1;
    margin-bottom: 0.3rem;
  }

  .stat-label {
    font-size: 0.95rem;
    color: var(--text-muted);
    font-weight: 600;
  }

  .stat-divider {
    width: 1px;
    height: 60px;
    background: var(--border);
  }

  /* Features Section */
  .features {
    background: var(--bg-secondary);
  }

  .section-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .section-header h2 {
    font-size: 2.2rem;
    font-weight: 800;
    margin-bottom: 0.8rem;
  }

  .section-header p {
    color: var(--text-muted);
    font-size: 1.15rem;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
  }

  .feature-card {
    background: var(--card);
    border-radius: 16px;
    overflow: hidden;
    border: 1px solid var(--border);
    transition: all 0.3s;
    text-decoration: none;
    color: var(--text);
    display: block;
  }

  .feature-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-lg);
    border-color: var(--primary);
  }

  .card-image {
    position: relative;
    height: 200px;
    overflow: hidden;
  }

  .card-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s;
  }

  .feature-card:hover .card-image img {
    transform: scale(1.08);
  }

  .card-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to bottom, transparent 0%, var(--card) 100%);
  }

  .card-content {
    padding: 2rem;
  }

  .card-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .feature-card h3 {
    font-size: 1.4rem;
    margin-bottom: 0.8rem;
    font-weight: 700;
  }

  .feature-card p {
    color: var(--text-secondary);
    font-size: 1rem;
    line-height: 1.7;
    margin-bottom: 1.2rem;
  }

  .card-list {
    list-style: none;
    margin-bottom: 1.5rem;
  }

  .card-list li {
    padding: 0.4rem 0;
    color: var(--text-muted);
    font-size: 0.95rem;
  }

  .card-link {
    color: var(--primary);
    font-weight: 700;
    font-size: 1.05rem;
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    transition: gap 0.3s;
  }

  .feature-card:hover .card-link {
    gap: 0.8rem;
  }

  /* Why Us Section */
  .why-us {
    background: var(--bg);
  }

  .why-grid {
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .why-content h2 {
    font-size: 2.2rem;
    font-weight: 800;
    margin-bottom: 1rem;
  }

  .why-desc {
    color: var(--text-secondary);
    font-size: 1.1rem;
    line-height: 1.8;
    margin-bottom: 2.5rem;
  }

  .why-features {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .why-item {
    display: flex;
    gap: 1.2rem;
    align-items: flex-start;
  }

  .why-icon {
    font-size: 2.2rem;
    flex-shrink: 0;
  }

  .why-item h4 {
    font-size: 1.15rem;
    margin-bottom: 0.4rem;
    font-weight: 700;
  }

  .why-item p {
    color: var(--text-muted);
    font-size: 0.95rem;
    line-height: 1.6;
  }

  .why-visual {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
  }

  .visual-card {
    background: var(--card);
    padding: 2.5rem 2rem;
    border-radius: 16px;
    text-align: center;
    border: 1px solid var(--border);
    transition: transform 0.3s;
  }

  .visual-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow);
  }

  .visual-icon {
    font-size: 2.8rem;
    margin-bottom: 1rem;
  }

  .visual-number {
    display: block;
    font-size: 2.5rem;
    font-weight: 900;
    color: var(--primary);
    line-height: 1;
    margin-bottom: 0.5rem;
  }

  .visual-label {
    color: var(--text-muted);
    font-size: 0.95rem;
    font-weight: 600;
  }

  /* Articles Section */
  .articles {
    background: var(--bg-secondary);
  }

  .articles-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }

  .article-card {
    background: var(--card);
    border-radius: 16px;
    overflow: hidden;
    border: 1px solid var(--border);
    transition: all 0.3s;
    text-decoration: none;
    color: var(--text);
  }

  .article-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-lg);
  }

  .article-image {
    position: relative;
    height: 200px;
    overflow: hidden;
  }

  .article-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s;
  }

  .article-card:hover .article-image img {
    transform: scale(1.05);
  }

  .article-badge {
    position: absolute;
    top: 1rem;
    right: 1rem;
    padding: 0.3rem 0.8rem;
    border-radius: 20px;
    font-size: 0.75rem;
    font-weight: 700;
    background: #10b981;
    color: white;
  }

  .article-badge.popular {
    background: #f59e0b;
  }

  .article-badge.updated {
    background: #3b82f6;
  }

  .article-content {
    padding: 1.5rem;
  }

  .article-meta {
    display: flex;
    justify-content: space-between;
    margin-bottom: 0.8rem;
    font-size: 0.85rem;
  }

  .tag {
    font-weight: 700;
    padding: 0.2rem 0.6rem;
    border-radius: 6px;
  }

  .tag.ai {
    background: rgba(59,130,246,0.1);
    color: #3b82f6;
  }

  .tag.security {
    background: rgba(239,68,68,0.1);
    color: #ef4444;
  }

  .tag.code {
    background: rgba(16,185,129,0.1);
    color: #10b981;
  }

  .read-time {
    color: var(--text-muted);
  }

  .article-content h3 {
    font-size: 1.15rem;
    margin-bottom: 0.6rem;
    font-weight: 700;
    line-height: 1.4;
  }

  .article-content p {
    color: var(--text-secondary);
    font-size: 0.95rem;
    line-height: 1.6;
  }

  /* Newsletter */
  .newsletter {
    background: var(--bg);
    padding: 4rem 0;
  }

  .newsletter-box {
    max-width: 600px;
    margin: 0 auto;
    text-align: center;
    background: var(--card);
    padding: 3rem 2rem;
    border-radius: 16px;
    border: 1px solid var(--border);
  }

  .newsletter-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .newsletter-box h2 {
    font-size: 1.8rem;
    margin-bottom: 0.8rem;
    font-weight: 700;
  }

  .newsletter-box p {
    color: var(--text-muted);
    margin-bottom: 2rem;
    font-size: 1rem;
  }

  .newsletter-form {
    display: flex;
    gap: 0.8rem;
    max-width: 450px;
    margin: 0 auto 1rem;
  }

  .newsletter-form input {
    flex: 1;
    padding: 0.9rem 1.2rem;
    border: 1px solid var(--border);
    border-radius: 12px;
    font-family: inherit;
    font-size: 1rem;
  }

  .newsletter-form input:focus {
    outline: none;
    border-color: var(--primary);
  }

  .newsletter-note {
    font-size: 0.85rem;
    color: var(--text-muted);
  }

  /* CTA Section */
  .cta-section {
    background: var(--bg-secondary);
    padding: 6rem 0;
  }

  .cta-box {
    max-width: 800px;
    margin: 0 auto;
    background: linear-gradient(135deg, var(--primary), var(--accent));
    padding: 4rem 3rem;
    border-radius: 20px;
    text-align: center;
    color: white;
    box-shadow: 0 20px 60px rgba(37,99,235,0.3);
  }

  .cta-content h2 {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    font-weight: 800;
  }

  .cta-content p {
    font-size: 1.2rem;
    opacity: 0.95;
    margin-bottom: 2.5rem;
  }

  .cta-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  /* Counter Animation */
  .counter {
    transition: all 0.3s;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .features-grid { grid-template-columns: 1fr; }
    .why-grid { grid-template-columns: 1fr; gap: 3rem; }
    .why-visual { grid-template-columns: repeat(2, 1fr); }
    .articles-grid { grid-template-columns: repeat(2, 1fr); }
  }

  @media (max-width: 768px) {
    .hero { min-height: auto; padding: 5rem 0 3rem; }
    .hero h1 { font-size: 2.2rem; }
    .hero-desc { font-size: 1.1rem; }
    
    .stats-grid { flex-direction: column; gap: 2rem; }
    .stat-divider { width: 100%; height: 1px; }
    
    .why-visual { grid-template-columns: 1fr; }
    .articles-grid { grid-template-columns: 1fr; }
    .cta-actions { flex-direction: column; }
    .btn { width: 100%; }
    
    .newsletter-form { flex-direction: column; }
    
    .section-header h2 { font-size: 1.8rem; }
    .container { padding: 0 1.2rem; }
  }
</style>

<script>
  // Counter Animation
  function initCounters() {
    const counters = document.querySelectorAll('.stat-number, .visual-number');
    counters.forEach(counter => {
      const target = parseInt(counter.dataset.target);
      const suffix = counter.dataset.suffix || '';
      const duration = 2000;
      const step = target / (duration / 16);
      let current = 0;
      
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            const timer = setInterval(() => {
              current += step;
              if (current >= target) {
                counter.textContent = target + suffix;
                clearInterval(timer);
              } else {
                counter.textContent = Math.floor(current) + suffix;
              }
            }, 16);
            observer.unobserve(entry.target);
          }
        });
      }, { threshold: 0.5 });
      
      observer.observe(counter);
    });
  }

  // Apply saved theme
  function initTheme() {
    const saved = localStorage.getItem('theme') || 'dark';
    document.documentElement.setAttribute('data-theme', saved);
  }

  document.addEventListener('DOMContentLoaded', () => {
    initTheme();
    initCounters();
  });
</script>
