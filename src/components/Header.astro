---
const measurementId = "G-6QW7XVX75J";
---

<header class="site-header">
  <div class="container">
    <!-- الشعار -->
    <a href="/Th-tech/" class="logo">
      <img src="/Th-tech/logo1.png" alt="Th-tech" class="logo-img" />
    </a>

    <!-- قائمة الكمبيوتر -->
    <nav class="nav-links">

      <a href="/Th-tech/" class="nav-link">
        <svg class="nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
          <polyline points="9 22 9 12 15 12 15 22"></polyline>
        </svg>
        <span>الرئيسية</span>
      </a>

      <a href="/Th-tech/tools/" class="nav-link">
        <svg class="nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a2 2 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"></path>
        </svg>
        <span>الأدوات</span>
      </a>

      <a href="/Th-tech/articles/" class="nav-link">
        <svg class="nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"></path>
          <path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 6.5 2z"></path>
        </svg>
        <span>المقالات</span>
      </a>

      <a href="/Th-tech/learning-paths/" class="nav-link">
        <svg class="nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="8" r="7"></circle>
          <polyline points="8.21 13.89 7 23 12 20 17 23 15.79 13.88"></polyline>
        </svg>
        <span>مسارات التعلم</span>
      </a>

      <a href="/Th-tech/category/" class="nav-link">
        <svg class="nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <rect x="3" y="3" width="7" height="7"></rect>
          <rect x="14" y="3" width="7" height="7"></rect>
          <rect x="14" y="14" width="7" height="7"></rect>
          <rect x="3" y="14" width="7" height="7"></rect>
        </svg>
        <span>التصنيفات</span>
      </a>

      <a href="/Th-tech/guides/" class="nav-link">
        <svg class="nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"></path>
          <path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"></path>
        </svg>
        <span>أدلة مرجعية</span>
      </a>

      <a href="/Th-tech/contact/" class="nav-link">
        <svg class="nav-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <rect x="2" y="4" width="20" height="16" rx="2"></rect>
          <polyline points="22,6 12,13 2,6"></polyline>
        </svg>
        <span>اتصل بنا</span>
      </a>

    </nav>

    <!-- أدوات الهيدر -->
    <div class="header-tools">

      <!-- البحث -->
      <button
        id="search-btn"
        class="tool-btn"
        title="بحث متقدم"
        aria-label="بحث"
      >
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="11" cy="11" r="8"></circle>
          <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
        </svg>
      </button>

      <!-- الوضع الليلي -->
      <button
        id="theme-btn"
        class="tool-btn"
        title="الوضع الليلي"
        aria-label="تبديل الوضع"
      >
        <svg
          class="icon-moon"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
        </svg>

        <svg
          class="icon-sun"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
        >
          <circle cx="12" cy="12" r="5"></circle>
          <line x1="12" y1="1" x2="12" y2="3"></line>
          <line x1="12" y1="21" x2="12" y2="23"></line>
          <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"></line>
          <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"></line>
          <line x1="1" y1="12" x2="3" y2="12"></line>
          <line x1="21" y1="12" x2="23" y2="12"></line>
          <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"></line>
          <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"></line>
        </svg>
      </button>

      <!-- القائمة -->
      <button
        id="menu-btn"
        class="hamburger-btn"
        title="القائمة"
        aria-label="فتح القائمة"
        aria-expanded="false"
      >
        <span class="hamburger-line"></span>
        <span class="hamburger-line"></span>
        <span class="hamburger-line"></span>
      </button>

    </div>
  </div>

  <!-- SEARCH PANEL -->
  <div id="search-panel" class="search-panel">
    <div class="search-container">

      <div class="search-icon-wrapper">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="11" cy="11" r="8"></circle>
          <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
        </svg>
      </div>

      <input
        type="text"
        id="searchInput"
        placeholder="ابحث في المقالات، التصنيفات، الكلمات المفتاحية..."
        class="search-input"
      />

      <button
        id="close-search"
        class="close-btn"
        aria-label="إغلاق البحث"
      >
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <line x1="18" y1="6" x2="6" y2="18"></line>
          <line x1="6" y1="6" x2="18" y2="18"></line>
        </svg>
      </button>

    </div>

    <div id="searchResults" class="search-results-box"></div>
    <div id="searchStats" class="search-stats"></div>
  </div>

  <!-- MOBILE MENU -->
  <nav id="mobile-menu" class="mobile-menu">

    <a href="/Th-tech/" class="mobile-link">
      <svg class="mobile-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
        <polyline points="9 22 9 12 15 12 15 22"></polyline>
      </svg>
      <span>الرئيسية</span>
    </a>

    <a href="/Th-tech/tools/" class="mobile-link">
      <svg class="mobile-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M14.7 6.3a1 1 0 0 0 0 1.4l1.6 1.6a2 2 0 0 0 1.4 0l3.77-3.77a6 6 0 0 1-7.94 7.94l-6.91 6.91a2.12 2.12 0 0 1-3-3l6.91-6.91a6 6 0 0 1 7.94-7.94l-3.76 3.76z"></path>
      </svg>
      <span>أدوات المطورين</span>
    </a>

    <a href="/Th-tech/articles/" class="mobile-link">
      <svg class="mobile-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"></path>
        <path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 6.5 2H20"></path>
      </svg>
      <span>المكتبة المعرفية</span>
    </a>

    <a href="/Th-tech/learning-paths/" class="mobile-link">
      <svg class="mobile-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <circle cx="12" cy="8" r="7"></circle>
        <polyline points="8.21 13.89 7 23 12 20 17 23 15.79 13.88"></polyline>
      </svg>
      <span>المسارات التعليمية</span>
    </a>

    <!-- CATEGORIES -->
    <div class="mobile-dropdown" id="categoriesDropdown">

      <button class="mobile-dropdown-toggle" aria-expanded="false">

        <span class="dropdown-text">

          <svg class="dropdown-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <rect x="3" y="3" width="7" height="7"></rect>
            <rect x="14" y="3" width="7" height="7"></rect>
            <rect x="14" y="14" width="7" height="7"></rect>
            <rect x="3" y="14" width="7" height="7"></rect>
          </svg>

          <span>الأقسام والتخصصات</span>

        </span>

        <svg class="dropdown-chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <polyline points="6 9 12 15 18 9"></polyline>
        </svg>

      </button>

      <ul class="dropdown-list" id="categoriesList">

        <li><a href="/Th-tech/windows/"><span>نظام Windows</span></a></li>
        <li><a href="/Th-tech/android/"><span>نظام Android</span></a></li>
        <li><a href="/Th-tech/ios/"><span>نظام iOS</span></a></li>
        <li><a href="/Th-tech/ai/"><span>الذكاء الاصطناعي</span></a></li>
        <li><a href="/Th-tech/programming/"><span>البرمجة والتطوير</span></a></li>
        <li><a href="/Th-tech/data-science/"><span>علم البيانات</span></a></li>
        <li><a href="/Th-tech/cloud/"><span>الحوسبة السحابية</span></a></li>
        <li><a href="/Th-tech/security/"><span>الأمن السيبراني</span></a></li>
        <li><a href="/Th-tech/devops/"><span>DevOps</span></a></li>
        <li><a href="/Th-tech/networks/"><span>الشبكات</span></a></li>
        <li><a href="/Th-tech/iot/"><span>إنترنت الأشياء</span></a></li>
        <li><a href="/Th-tech/5g-technology/"><span>تقنية 5G</span></a></li>
        <li><a href="/Th-tech/blockchain/"><span>البلوك تشين</span></a></li>
        <li><a href="/Th-tech/quantum-computing/"><span>الحوسبة الكمية</span></a></li>
        <li><a href="/Th-tech/edge-computing/"><span>الحوسبة الطرفية</span></a></li>
        <li><a href="/Th-tech/mobile/"><span>تطوير الجوال</span></a></li>

      </ul>
    </div>

    <!-- GUIDES -->
    <div class="mobile-dropdown" id="guidesDropdown">

      <button class="mobile-dropdown-toggle" aria-expanded="false">

        <span class="dropdown-text">

          <svg class="dropdown-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"></path>
            <path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 0 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"></path>
          </svg>

          <span>الأدلة التقنية الشاملة</span>

        </span>

        <svg class="dropdown-chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <polyline points="6 9 12 15 18 9"></polyline>
        </svg>

      </button>

      <ul class="dropdown-list" id="guidesList">

        <li><a href="/Th-tech/guides/linux/terminal/"><span>Linux وسطر الأوامر</span></a></li>
        <li><a href="/Th-tech/guides/docker/handbook/"><span>Docker والحاويات</span></a></li>
        <li><a href="/Th-tech/guides/git/cheatsheet/"><span>Git وإدارة النسخ</span></a></li>
        <li><a href="/Th-tech/guides/nginx/config/"><span>Nginx والخوادم</span></a></li>
        <li><a href="/Th-tech/guides/vscode/setup/"><span>VS Code والإنتاجية</span></a></li>
        <li><a href="/Th-tech/guides/shell/automation/"><span>Shell Scripting</span></a></li>
        <li><a href="/Th-tech/guides/http-api/rest/"><span>HTTP & APIs</span></a></li>
        <li><a href="/Th-tech/guides/sql/queries/"><span>SQL وقواعد البيانات</span></a></li>
        <li><a href="/Th-tech/guides/css/layout/"><span>CSS وتخطيط الواجهات</span></a></li>
        <li><a href="/Th-tech/guides/js/patterns/"><span>JavaScript أنماط متقدمة</span></a></li>
        <li><a href="/Th-tech/guides/python/automation/"><span>Python للأتمتة</span></a></li>
        <li><a href="/Th-tech/guides/cicd/pipeline/"><span>CI/CD و DevOps</span></a></li>
        <li><a href="/Th-tech/guides/security/crypto/"><span>الأمان والتشفير</span></a></li>
        <li><a href="/Th-tech/guides/networking/basics/"><span>أساسيات الشبكات</span></a></li>
        <li><a href="/Th-tech/guides/regex/patterns/"><span>Regex ومعالجة النصوص</span></a></li>
        <li><a href="/Th-tech/guides/markdown/docs/"><span>Markdown والتوثيق</span></a></li>

      </ul>
    </div>

    <a href="/Th-tech/guides/" class="mobile-link mobile-link-primary">
      <svg class="mobile-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"></path>
      </svg>
      <span>استكشف جميع الأدلة</span>
    </a>

    <a href="/Th-tech/contact/" class="mobile-link">
      <svg class="mobile-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <rect x="2" y="4" width="20" height="16" rx="2"></rect>
        <polyline points="22,6 12,13 2,6"></polyline>
      </svg>
      <span>تواصل معنا</span>
    </a>

    <!-- SOCIAL -->
    <div class="mobile-social-section">

      <p class="mobile-social-title">تابعنا على</p>

      <div class="mobile-social-links">

        <!-- X -->
        <a
          href="https://x.com/thtechma"
          target="_blank"
          rel="noopener noreferrer"
          class="mobile-social-link"
          title="X"
          aria-label="X - Th-Tech"
        >
          <svg viewBox="0 0 24 24" fill="currentColor">
            <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/>
          </svg>
        </a>

        <!-- Facebook -->
        <a
          href="https://www.facebook.com/share/1PzewvX774/"
          target="_blank"
          rel="noopener noreferrer"
          class="mobile-social-link"
          title="Facebook"
          aria-label="فيسبوك - Th-Tech"
        >
          <svg viewBox="0 0 24 24" fill="currentColor">
            <path d="M24 12.073C24 5.405 18.627 0 12 0S0 5.405 0 12.073c0 6.017 4.388 11.002 10.125 11.925v-8.433H7.078v-3.492h3.047V9.409c0-3.032 1.791-4.71 4.533-4.71 1.312 0 2.686.235 2.686.235v2.973h-1.514c-1.491 0-1.956.932-1.956 1.887v2.279h3.328l-.532 3.492h-2.796v8.433C19.612 23.075 24 18.09 24 12.073z"/>
          </svg>
        </a>

        <!-- Instagram -->
        <a
          href="https://www.instagram.com/thtech.ma"
          target="_blank"
          rel="noopener noreferrer"
          class="mobile-social-link"
          title="Instagram"
          aria-label="إنستغرام - Th-Tech"
        >
          <svg viewBox="0 0 24 24" fill="currentColor">
            <path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.28-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163C8.741 0 8.333.014 7.053.072 2.695.272.273 2.69.073 7.052.014 8.333 0 8.741 0 12s.014 3.668.072 4.948c.2 4.358 2.618 6.78 6.98 6.98 1.28.058 1.688.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618-6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.667.014 15.259 0 12 0zm0 5.838A6.162 6.162 0 1 0 18.162 12 6.162 6.162 0 0 0 12 5.838zm0 10.162A4 4 0 1 1 16 12a4 4 0 0 1-4 4zm6.406-11.845a1.44 1.44 0 1 0 0 2.88 1.44 1.44 0 0 0 0-2.88z"/>
          </svg>
        </a>

        <!-- LinkedIn -->
        <a
          href="https://www.linkedin.com/in/th-tech-المنصة-التقنية-341764438?utm_source=share_via&utm_content=profile&utm_medium=member_android"
          target="_blank"
          rel="noopener noreferrer"
          class="mobile-social-link"
          title="LinkedIn"
          aria-label="لينكد إن - Th-Tech"
        >
          <svg viewBox="0 0 24 24" fill="currentColor">
            <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792.774 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
          </svg>
        </a>

        <!-- YouTube -->
        <a
          href="https://www.youtube.com/@thtechma"
          target="_blank"
          rel="noopener noreferrer"
          class="mobile-social-link"
          title="YouTube"
          aria-label="يوتيوب - Th-Tech"
        >
          <svg viewBox="0 0 24 24" fill="currentColor">
            <path d="M23.498 6.186a3.016 3.016 0 0 0-2.122-2.136C19.505 3.545 12 3.545 12 3.545s-7.505 0-9.377.505A3.017 3.017 0 0 0 .502 6.186C0 8.07 0 12 0 12s0 3.93.502 5.814a3.016 3.016 0 0 0 2.122 2.136c1.871.505 9.376.505 9.376.505s7.505 0 9.377-.505a3.015 3.015 0 0 0 2.122-2.136C24 15.93 24 12 24 12s0-3.93-.502-5.814zM9.545 15.568V8.432L15.818 12l-6.273 3.568z"/>
          </svg>
        </a>

      </div>

      <!-- البريد الرسمي -->
      <a
        href="mailto:thtech.tech@gmail.com"
        class="mobile-email-link"
        aria-label="البريد الإلكتروني Th-Tech"
      >
        ✉ thtech.tech@gmail.com
      </a>

    </div>

    <div class="mobile-footer">
      <p>Th-Tech © 2026</p>
    </div>

  </nav>
</header>

<!-- =====================================================
     GOOGLE ANALYTICS — GA4
     ===================================================== -->

<script
  async
  src="https://www.googletagmanager.com/gtag/js?id=G-6QW7XVX75J"
></script>

<script is:inline>
  window.dataLayer = window.dataLayer || [];

  function gtag() {
    window.dataLayer.push(arguments);
  }

  gtag("js", new Date());

  gtag("config", "G-6QW7XVX75J", {
    send_page_view: true
  });
</script>

<style>
/* =====================================================
   GLOBAL RESET
===================================================== */

@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap');

:root {
  --bg: #f8fafc;
  --card: #ffffff;
  --text: #0f172a;
  --text-sec: #64748b;
  --primary: #0284c7;
  --primary-light: #38bdf8;
  --border: rgba(2,132,199,0.2);
  --shadow: rgba(0,0,0,0.1);
  --font-main: 'Inter','Segoe UI',system-ui,-apple-system,sans-serif;
}

html.dark-mode {
  --bg: #0b1120;
  --card: #1e293b;
  --text: #f1f5f9;
  --text-sec: #94a3b8;
  --primary: #38bdf8;
  --primary-light: #0ea5e9;
  --border: rgba(56,189,248,0.2);
  --shadow: rgba(0,0,0,0.3);
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html,
body {
  width: 100%;
  min-width: 100%;
  max-width: 100%;
  overflow-x: hidden;
}

body {
  background: var(--bg);
  color: var(--text);
  font-family: var(--font-main);
}

/* =====================================================
   HEADER — FULL WIDTH DESKTOP
===================================================== */

.site-header {
  position: sticky;
  top: 0;
  z-index: 1000;
  width: 100%;
  max-width: none;
  background: var(--card);
  border-bottom: 2px solid var(--border);
  padding: 0.5rem 0;
  box-shadow: 0 2px 20px var(--shadow);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
}

/* =====================================================
   DESKTOP CONTAINER
===================================================== */

.container {
  width: 100%;
  max-width: none;
  margin: 0;
  padding: 0 2.5rem;
  height: 80px;
  display: flex;
  align-items: center;
  direction: ltr;
  gap: 1.4rem;
}

/* =====================================================
   LOGO
===================================================== */

.logo {
  flex: 0 0 auto;
  display: flex;
  align-items: center;
  margin: 0;
  text-decoration: none;
  transition: transform 0.3s ease;
}

.logo:hover {
  transform: scale(1.05);
}

.logo-img {
  height: 48px;
  width: auto;
  display: block;
}

/* =====================================================
   DESKTOP NAV
===================================================== */

.nav-links {
  flex: 1 1 auto;
  min-width: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.35rem;
  direction: rtl;
}

.nav-link {
  color: var(--text);
  text-decoration: none;
  font-weight: 600;
  font-size: 0.86rem;
  padding: 0.5rem 0.65rem;
  border-radius: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.4rem;
  white-space: nowrap;
  transition: all 0.3s ease;
}

.nav-icon {
  width: 18px;
  height: 18px;
  flex-shrink: 0;
  stroke-width: 2.2;
}

.nav-link:hover {
  color: var(--primary);
  background: rgba(2,132,199,0.1);
  transform: translateY(-2px);
}

/* =====================================================
   HEADER TOOLS
===================================================== */

.header-tools {
  flex: 0 0 auto;
  display: flex;
  align-items: center;
  gap: 0.55rem;
  margin: 0;
  direction: ltr;
}

.tool-btn {
  width: 42px;
  height: 42px;
  flex: 0 0 42px;
  background: linear-gradient(135deg, var(--primary), var(--primary-light));
  border: none;
  color: white;
  border-radius: 12px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition:
    transform 0.3s cubic-bezier(0.4,0,0.2,1),
    box-shadow 0.3s ease;
  box-shadow: 0 3px 10px rgba(2,132,199,0.3);
}

.tool-btn svg {
  width: 20px;
  height: 20px;
  stroke-width: 2.2;
}

.tool-btn:hover {
  transform: translateY(-3px) scale(1.05);
  box-shadow: 0 6px 20px rgba(2,132,199,0.4);
}

.tool-btn:active {
  transform: scale(0.95);
}

/* =====================================================
   THEME ICONS
===================================================== */

.icon-sun {
  display: none;
}

html.dark-mode .icon-moon {
  display: none;
}

html.dark-mode .icon-sun {
  display: block;
}

/* =====================================================
   HAMBURGER
===================================================== */

.hamburger-btn {
  display: none;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  gap: 5px;
  width: 44px;
  height: 44px;
  background: linear-gradient(135deg, var(--primary), var(--primary-light));
  border: none;
  border-radius: 12px;
  cursor: pointer;
  transition: all 0.3s cubic-bezier(0.4,0,0.2,1);
  box-shadow: 0 3px 10px rgba(2,132,199,0.3);
  padding: 10px;
}

.hamburger-btn:hover {
  transform: translateY(-3px) scale(1.05);
  box-shadow: 0 6px 20px rgba(2,132,199,0.4);
}

.hamburger-btn:active {
  transform: scale(0.95);
}

.hamburger-line {
  width: 22px;
  height: 2.5px;
  background: white;
  border-radius: 3px;
  transition: all 0.3s cubic-bezier(0.4,0,0.2,1);
  transform-origin: center;
}

.hamburger-btn[aria-expanded="true"] .hamburger-line:nth-child(1) {
  transform: rotate(45deg) translate(5px,5px);
}

.hamburger-btn[aria-expanded="true"] .hamburger-line:nth-child(2) {
  opacity: 0;
  transform: scale(0.8);
}

.hamburger-btn[aria-expanded="true"] .hamburger-line:nth-child(3) {
  transform: rotate(-45deg) translate(5px,-5px);
}

/* =====================================================
   SEARCH PANEL
===================================================== */

.search-panel {
  display: none;
  position: absolute;
  top: 80px;
  left: 0;
  right: 0;
  width: 100%;
  background: var(--card);
  padding: 2rem 1.5rem;
  border-bottom: 2px solid var(--border);
  z-index: 999;
  box-shadow: 0 10px 40px var(--shadow);
  animation: slideDown 0.3s ease;
}

@keyframes slideDown {
  from {
    opacity: 0;
    transform: translateY(-20px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.search-panel.active {
  display: block !important;
}

.search-container {
  max-width: 700px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  gap: 1rem;
  background: var(--bg);
  padding: 0.8rem 1.5rem;
  border-radius: 16px;
  border: 2px solid var(--border);
  transition: all 0.3s;
}

.search-container:focus-within {
  border-color: var(--primary);
  box-shadow: 0 0 0 4px rgba(2,132,199,0.1);
  transform: translateY(-2px);
}

.search-icon-wrapper {
  color: var(--primary);
  flex-shrink: 0;
}

.search-icon-wrapper svg {
  width: 22px;
  height: 22px;
}

.search-input {
  flex: 1;
  border: none;
  background: transparent;
  color: var(--text);
  font-size: 1.05rem;
  font-family: var(--font-main);
  outline: none;
}

.search-input::placeholder {
  color: var(--text-sec);
}

.close-btn {
  background: #ef4444;
  color: white;
  border: none;
  width: 40px;
  height: 40px;
  border-radius: 12px;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.3s;
  flex-shrink: 0;
}

.close-btn:hover {
  background: #dc2626;
  transform: rotate(90deg) scale(1.1);
}

.close-btn svg {
  width: 20px;
  height: 20px;
}

/* =====================================================
   SEARCH RESULTS
===================================================== */

.search-results-box {
  max-width: 700px;
  margin: 15px auto 0;
  background: var(--card);
  border-radius: 12px;
  max-height: 400px;
  overflow-y: auto;
  box-shadow: 0 4px 20px var(--shadow);
  border: 1px solid var(--border);
}

.search-result-link {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 15px 20px;
  text-decoration: none;
  color: var(--text);
  border-bottom: 1px solid var(--border);
  transition: all 0.3s;
  background: var(--card);
  font-family: var(--font-main);
}

.search-result-link:hover {
  background: linear-gradient(
    135deg,
    rgba(2,132,199,0.1),
    rgba(56,189,248,0.1)
  );
  color: var(--primary);
  padding-right: 25px;
  transform: translateX(5px);
}

.search-result-link:last-child {
  border-bottom: none;
}

.result-title {
  font-weight: 600;
  font-size: 1rem;
}

.result-category {
  font-size: 0.85rem;
  color: var(--text-sec);
  background: var(--bg);
  padding: 4px 10px;
  border-radius: 6px;
}

.result-highlight {
  background: linear-gradient(
    120deg,
    rgba(2,132,199,0.3),
    rgba(56,189,248,0.3)
  );
  padding: 2px 6px;
  border-radius: 4px;
  font-weight: 700;
}

.no-results {
  padding: 40px 20px;
  text-align: center;
  color: var(--text-sec);
  font-size: 1.05rem;
}

.search-stats {
  max-width: 700px;
  margin: 10px auto 0;
  text-align: center;
  color: var(--text-sec);
  font-size: 0.9rem;
  padding: 0.5rem;
}

/* =====================================================
   MOBILE MENU
===================================================== */

.mobile-menu {
  display: none;
  position: absolute;
  top: 80px;
  left: 0;
  right: 0;
  width: 100%;
  background: var(--card);
  padding: 0.8rem 1.2rem;
  border-bottom: 2px solid var(--border);
  flex-direction: column;
  z-index: 999;
  box-shadow: 0 10px 40px var(--shadow);
  animation: slideDown 0.3s ease;
  gap: 0.2rem;
  max-height: calc(100vh - 80px);
  overflow-y: auto;
}

.mobile-menu.active {
  display: flex;
}

.mobile-link {
  color: var(--text);
  text-decoration: none;
  padding: 0.7rem 1rem;
  border-bottom: 1px solid var(--border);
  font-weight: 600;
  font-size: 0.95rem;
  transition: all 0.3s;
  border-radius: 8px;
  display: flex;
  align-items: center;
  gap: 0.8rem;
}

.mobile-link-primary {
  background: linear-gradient(
    135deg,
    rgba(2,132,199,0.1),
    rgba(56,189,248,0.1)
  );
  border: 1.5px solid var(--border);
  margin: 0.3rem 0;
}

.mobile-icon {
  width: 19px;
  height: 19px;
  flex-shrink: 0;
  stroke-width: 2.2;
}

.mobile-link:hover {
  background: var(--bg);
  color: var(--primary);
  padding-right: 1.5rem;
}

/* =====================================================
   DROPDOWNS
===================================================== */

.mobile-dropdown {
  width: 100%;
  margin: 0.1rem 0;
}

.mobile-dropdown-toggle {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: transparent;
  border: none;
  color: var(--text);
  font-size: 0.95rem;
  font-weight: 600;
  padding: 0.7rem 1rem;
  cursor: pointer;
  border-radius: 8px;
  border-bottom: 1px solid var(--border);
  font-family: var(--font-main);
}

.mobile-dropdown-toggle:hover,
.mobile-dropdown-toggle[aria-expanded="true"] {
  background: rgba(2,132,199,0.1);
  color: var(--primary);
}

.dropdown-text {
  display: flex;
  align-items: center;
  gap: 0.8rem;
}

.dropdown-icon {
  width: 19px;
  height: 19px;
  stroke-width: 2.2;
}

.dropdown-chevron {
  width: 18px;
  height: 18px;
  transition: transform 0.3s ease;
  color: var(--text-sec);
  stroke-width: 2.2;
}

.mobile-dropdown-toggle[aria-expanded="true"] .dropdown-chevron {
  transform: rotate(180deg);
  color: var(--primary);
}

.dropdown-list {
  list-style: none;
  padding: 0;
  margin: 0;
  max-height: 0;
  overflow: hidden;
  transition:
    max-height 0.35s cubic-bezier(0.4,0,0.2,1),
    opacity 0.25s ease;
  opacity: 0;
  background: rgba(2,132,199,0.03);
  border-radius: 0 0 8px 8px;
}

.dropdown-list.open {
  max-height: 60vh;
  opacity: 1;
  overflow-y: auto;
}

.dropdown-list li a {
  display: flex;
  align-items: center;
  gap: 0.7rem;
  padding: 0.6rem 1.2rem 0.6rem 2.2rem;
  color: var(--text-sec);
  text-decoration: none;
  font-size: 0.9rem;
  border-bottom: 1px solid var(--border);
  transition: all 0.2s;
}

.dropdown-list li:last-child a {
  border-bottom: none;
}

.dropdown-list li a:hover {
  background: rgba(2,132,199,0.1);
  color: var(--primary);
  padding-right: 1.5rem;
}

.cat-icon {
  width: 18px;
  height: 18px;
  flex-shrink: 0;
  color: var(--primary);
  stroke-width: 2.2;
}

/* =====================================================
   SOCIAL
===================================================== */

.mobile-social-section {
  margin: 0.6rem 0 0.3rem;
  padding: 0.9rem;
  background: linear-gradient(
    135deg,
    rgba(2,132,199,0.05),
    rgba(56,189,248,0.05)
  );
  border-radius: 10px;
  border: 1.5px solid var(--border);
}

.mobile-social-title {
  text-align: center;
  color: var(--text);
  font-weight: 700;
  font-size: 0.9rem;
  margin-bottom: 0.7rem;
}

.mobile-social-links {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 0.6rem;
  flex-wrap: wrap;
}

.mobile-social-link {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 38px;
  height: 38px;
  border-radius: 10px;
  background: var(--card);
  color: var(--text-sec);
  transition: all 0.3s;
  text-decoration: none;
  border: 1.5px solid var(--border);
}

.mobile-social-link svg {
  width: 18px;
  height: 18px;
}

.mobile-social-link:hover {
  background: linear-gradient(
    135deg,
    var(--primary),
    var(--primary-light)
  );
  color: white;
  transform: translateY(-3px) scale(1.05);
}

/* البريد الرسمي */
.mobile-email-link {
  display: block;
  margin-top: 0.8rem;
  text-align: center;
  color: var(--primary);
  text-decoration: none;
  font-size: 0.82rem;
  font-weight: 600;
  direction: ltr;
  transition: all 0.3s ease;
}

.mobile-email-link:hover {
  opacity: 0.8;
  transform: translateY(-1px);
}

/* =====================================================
   MOBILE FOOTER
===================================================== */

.mobile-footer {
  text-align: center;
  padding: 0.8rem 0.5rem;
  margin-top: 0.3rem;
  border-top: 2px solid var(--border);
  color: var(--text-sec);
  font-size: 0.8rem;
}

.mobile-footer p {
  margin: 0.1rem 0;
  font-weight: 600;
}

/* =====================================================
   DESKTOP — MEDIUM
===================================================== */

@media (max-width: 1200px) {

  .container {
    padding-left: 1.5rem;
    padding-right: 1.5rem;
    gap: 0.8rem;
  }

  .nav-links {
    gap: 0.1rem;
  }

  .nav-link {
    font-size: 0.78rem;
    padding: 0.4rem 0.45rem;
  }

}

/* =====================================================
   TABLET
===================================================== */

@media (max-width: 900px) {

  .nav-link span {
    display: none;
  }

  .nav-icon {
    width: 19px;
    height: 19px;
  }

}

/* =====================================================
   MOBILE
===================================================== */

@media (max-width: 768px) {

  .nav-links {
    display: none;
  }

  .hamburger-btn {
    display: flex;
  }

  .container {
    width: 100%;
    height: 72px;
    padding: 0 1.3rem;
    direction: ltr;
    gap: 0;
  }

  .logo {
    margin-right: auto;
  }

  .header-tools {
    margin-left: auto;
    gap: 0.45rem;
  }

  .tool-btn {
    width: 40px;
    height: 40px;
    flex-basis: 40px;
    border-radius: 10px;
  }

  .search-panel,
  .mobile-menu {
    top: 72px;
    max-height: calc(100vh - 72px);
  }

  .search-container {
    padding: 0.7rem 1.2rem;
    border-radius: 14px;
  }

  .search-input {
    font-size: 0.98rem;
  }

  .logo-img {
    height: 44px;
  }

}

/* =====================================================
   SMALL MOBILE
===================================================== */

@media (max-width: 480px) {

  .container {
    height: 68px;
    padding: 0 1rem;
  }

  .header-tools {
    gap: 0.3rem;
  }

  .tool-btn {
    width: 38px;
    height: 38px;
    flex-basis: 38px;
  }

  .hamburger-btn {
    width: 40px;
    height: 40px;
  }

  .search-panel,
  .mobile-menu {
    top: 68px;
    max-height: calc(100vh - 68px);
  }

  .logo-img {
    height: 40px;
  }

  .mobile-social-link {
    width: 36px;
    height: 36px;
  }

  .mobile-social-link svg {
    width: 17px;
    height: 17px;
  }

  .mobile-menu {
    padding: 0.6rem 1rem;
    gap: 0.15rem;
  }

  .mobile-link {
    padding: 0.6rem 0.9rem;
    font-size: 0.92rem;
  }

  .mobile-dropdown-toggle {
    padding: 0.6rem 0.9rem;
    font-size: 0.92rem;
  }

}
</style>

<script>
  /* =====================================================
     SEARCH DATA
  ===================================================== */

  const siteContent = {

    articles: [

      {
        title: "الذكاء الاصطناعي",
        url: "/Th-tech/ai/",
        category: "تصنيف",
        keywords: [
          "ai",
          "machine learning",
          "تعلم آلي"
        ]
      },

      {
        title: "الشبكات",
        url: "/Th-tech/networks/",
        category: "تصنيف",
        keywords: [
          "network",
          "router",
          "switch"
        ]
      },

      {
        title: "البرمجة",
        url: "/Th-tech/programming/",
        category: "تصنيف",
        keywords: [
          "code",
          "software",
          "development"
        ]
      },

      {
        title: "الأمن السيبراني",
        url: "/Th-tech/security/",
        category: "تصنيف",
        keywords: [
          "security",
          "cyber",
          "hacking"
        ]
      },

      {
        title: "علم البيانات",
        url: "/Th-tech/data-science/",
        category: "تصنيف",
        keywords: [
          "data",
          "analytics",
          "statistics"
        ]
      },

      {
        title: "DevOps",
        url: "/Th-tech/devops/",
        category: "تصنيف",
        keywords: [
          "devops",
          "ci/cd",
          "automation"
        ]
      },

      {
        title: "IoT",
        url: "/Th-tech/iot/",
        category: "تصنيف",
        keywords: [
          "internet of things",
          "smart devices"
        ]
      },

      {
        title: "Cloud",
        url: "/Th-tech/cloud/",
        category: "تصنيف",
        keywords: [
          "cloud computing",
          "aws",
          "azure"
        ]
      },

      {
        title: "Blockchain",
        url: "/Th-tech/blockchain/",
        category: "تصنيف",
        keywords: [
          "blockchain",
          "crypto",
          "bitcoin"
        ]
      },

      {
        title: "Mobile",
        url: "/Th-tech/mobile/",
        category: "تصنيف",
        keywords: [
          "mobile",
          "android",
          "ios"
        ]
      }

    ],

    generalKeywords: [

      {
        word: "tutorial",
        title: "دروس تعليمية",
        url: "/Th-tech/articles/"
      },

      {
        word: "guide",
        title: "أدلة شاملة",
        url: "/Th-tech/articles/"
      },

      {
        word: "beginner",
        title: "للمبتدئين",
        url: "/Th-tech/category/"
      },

      {
        word: "advanced",
        title: "مستوى متقدم",
        url: "/Th-tech/category/"
      }

    ]

  };

  /* =====================================================
     THEME
  ===================================================== */

  function toggleTheme() {

    const html = document.documentElement;
    const btn = document.getElementById('theme-btn');

    html.classList.toggle('dark-mode');

    if (html.classList.contains('dark-mode')) {

      localStorage.setItem('theme', 'dark');

      btn.setAttribute(
        'title',
        'الوضع النهاري'
      );

    } else {

      localStorage.setItem('theme', 'light');

      btn.setAttribute(
        'title',
        'الوضع الليلي'
      );

    }

  }

  /* =====================================================
     HIGHLIGHT
  ===================================================== */

  function highlightText(text, query) {

    if (!query) return text;

    const escapedQuery =
      query.replace(
        /[.*+?^${}()|[\]\\]/g,
        '\\$&'
      );

    const regex =
      new RegExp(
        `(${escapedQuery})`,
        'gi'
      );

    return text.replace(
      regex,
      '<span class="result-highlight">$1</span>'
    );

  }

  /* =====================================================
     SEARCH
  ===================================================== */

  function performAdvancedSearch(query) {

    const results = [];

    const queryLower =
      query.toLowerCase().trim();

    siteContent.articles.forEach(
      article => {

        const titleMatch =
          article.title
            .toLowerCase()
            .includes(queryLower);

        const categoryMatch =
          article.category
            .toLowerCase()
            .includes(queryLower);

        const keywordMatch =
          article.keywords.some(
            k =>
              k.toLowerCase()
               .includes(queryLower)
          );

        if (
          titleMatch ||
          categoryMatch ||
          keywordMatch
        ) {

          results.push({

            ...article,

            matchType:
              titleMatch
                ? 'عنوان'
                : (
                    categoryMatch
                      ? 'تصنيف'
                      : 'كلمة مفتاحية'
                  )

          });

        }

      }
    );

    siteContent.generalKeywords.forEach(
      item => {

        if (
          item.word
            .toLowerCase()
            .includes(queryLower) ||
          item.title
            .includes(queryLower)
        ) {

          results.push({

            title: item.title,
            url: item.url,
            category: 'كلمة مفتاحية',
            matchType: 'عام'

          });

        }

      }
    );

    return results;

  }

  /* =====================================================
     SCROLL REVEAL
  ===================================================== */

  function initScrollReveal() {

    const reveals =
      document.querySelectorAll(
        '.reveal, .reveal-from-left, .reveal-from-right, .reveal-scale'
      );

    const revealOnScroll = () => {

      const windowHeight =
        window.innerHeight;

      const elementVisible = 150;

      reveals.forEach(
        reveal => {

          const elementTop =
            reveal.getBoundingClientRect().top;

          if (
            elementTop <
            windowHeight - elementVisible
          ) {

            reveal.classList.add('active');

          }

        }
      );

    };

    window.addEventListener(
      'scroll',
      revealOnScroll
    );

    revealOnScroll();

  }

  /* =====================================================
     INITIALIZATION
  ===================================================== */

  document.addEventListener(
    'DOMContentLoaded',
    () => {

      /* THEME */

      const savedTheme =
        localStorage.getItem('theme');

      const themeBtn =
        document.getElementById('theme-btn');

      if (
        savedTheme === 'dark'
      ) {

        document.documentElement
          .classList
          .add('dark-mode');

        themeBtn.setAttribute(
          'title',
          'الوضع النهاري'
        );

      }

      themeBtn.addEventListener(
        'click',
        toggleTheme
      );

      /* MOBILE MENU */

      const menuBtn =
        document.getElementById('menu-btn');

      const mobileMenu =
        document.getElementById('mobile-menu');

      menuBtn.addEventListener(
        'click',
        () => {

          const isExpanded =
            menuBtn.getAttribute(
              'aria-expanded'
            ) === 'true';

          menuBtn.setAttribute(
            'aria-expanded',
            !isExpanded
          );

          mobileMenu.classList.toggle(
            'active'
          );

        }
      );

      /* DROPDOWNS */

      function setupDropdown(
        dropdownId
      ) {

        const dropdown =
          document.getElementById(
            dropdownId
          );

        if (!dropdown) return;

        const toggleBtn =
          dropdown.querySelector(
            '.mobile-dropdown-toggle'
          );

        const dropdownList =
          dropdown.querySelector(
            '.dropdown-list'
          );

        if (
          toggleBtn &&
          dropdownList
        ) {

          toggleBtn.addEventListener(
            'click',
            e => {

              e.preventDefault();

              const isExpanded =
                toggleBtn.getAttribute(
                  'aria-expanded'
                ) === 'true';

              toggleBtn.setAttribute(
                'aria-expanded',
                !isExpanded
              );

              dropdownList.classList.toggle(
                'open'
              );

            }
          );

          dropdownList
            .querySelectorAll('a')
            .forEach(
              link => {

                link.addEventListener(
                  'click',
                  () => {

                    toggleBtn.setAttribute(
                      'aria-expanded',
                      'false'
                    );

                    dropdownList.classList.remove(
                      'open'
                    );

                  }
                );

              }
            );

        }

      }

      setupDropdown(
        'categoriesDropdown'
      );

      setupDropdown(
        'guidesDropdown'
      );

      /* SEARCH */

      const searchBtn =
        document.getElementById(
          'search-btn'
        );

      const searchPanel =
        document.getElementById(
          'search-panel'
        );

      const closeSearch =
        document.getElementById(
          'close-search'
        );

      const searchInput =
        document.getElementById(
          'searchInput'
        );

      const searchResults =
        document.getElementById(
          'searchResults'
        );

      const searchStats =
        document.getElementById(
          'searchStats'
        );

      searchBtn.addEventListener(
        'click',
        () => {

          searchPanel.classList.add(
            'active'
          );

          setTimeout(
            () =>
              searchInput.focus(),
            100
          );

        }
      );

      closeSearch.addEventListener(
        'click',
        () => {

          searchPanel.classList.remove(
            'active'
          );

          searchInput.value = '';
          searchResults.innerHTML = '';
          searchStats.textContent = '';

        }
      );

      let searchTimeout;

      searchInput.addEventListener(
        'input',
        function(e) {

          clearTimeout(
            searchTimeout
          );

          const query =
            e.target.value.trim();

          if (
            query.length < 2
          ) {

            searchResults.innerHTML = '';
            searchStats.textContent = '';

            return;

          }

          searchTimeout =
            setTimeout(
              () => {

                const results =
                  performAdvancedSearch(
                    query
                  );

                if (
                  results.length > 0
                ) {

                  searchResults.innerHTML =
                    results
                      .map(
                        r => `
                          <a
                            href="${r.url}"
                            class="search-result-link"
                          >
                            <span class="result-title">
                              ${highlightText(
                                r.title,
                                query
                              )}
                            </span>

                            <span class="result-category">
                              ${r.category}
                            </span>
                          </a>
                        `
                      )
                      .join('');

                  searchStats.textContent =
                    `تم العثور على ${results.length} نتيجة لـ "${query}"`;

                } else {

                  searchResults.innerHTML =
                    '<div class="no-results">😕 لم يتم العثور على نتائج. جرب كلمات أخرى</div>';

                  searchStats.textContent = '';

                }

              },
              300
            );

        }
      );

      /* ESCAPE */

      document.addEventListener(
        'keydown',
        e => {

          if (
            e.key === 'Escape'
          ) {

            searchPanel.classList.remove(
              'active'
            );

            mobileMenu.classList.remove(
              'active'
            );

            menuBtn.setAttribute(
              'aria-expanded',
              'false'
            );

            document
              .querySelectorAll(
                '.mobile-dropdown-toggle'
              )
              .forEach(
                btn => {

                  btn.setAttribute(
                    'aria-expanded',
                    'false'
                  );

                  const dropdown =
                    btn.closest(
                      '.mobile-dropdown'
                    );

                  if (dropdown) {

                    dropdown
                      .querySelector(
                        '.dropdown-list'
                      )
                      ?.classList
                      .remove('open');

                  }

                }
              );

            searchInput.value = '';
            searchResults.innerHTML = '';

          }

        }
      );

      /* OUTSIDE CLICK */

      document.addEventListener(
        'click',
        e => {

          if (
            !searchPanel.contains(e.target) &&
            !searchBtn.contains(e.target)
          ) {

            searchPanel.classList.remove(
              'active'
            );

          }

          if (
            !mobileMenu.contains(e.target) &&
            !menuBtn.contains(e.target) &&
            mobileMenu.classList.contains(
              'active'
            )
          ) {

            mobileMenu.classList.remove(
              'active'
            );

            menuBtn.setAttribute(
              'aria-expanded',
              'false'
            );

          }

        }
      );

      initScrollReveal();

    }
  );
</script>
