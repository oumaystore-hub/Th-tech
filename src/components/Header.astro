---
---
<header class="site-header">
  <div class="container">
    <a href="/Th-tech/" class="logo">
      <img src="/Th-tech/logo1.png" alt="Th-tech" class="logo-img" />
    </a>
    
    <nav class="nav-links">
      <a href="/Th-tech/" class="nav-link">الرئيسية</a>
      <a href="/Th-tech/articles/" class="nav-link">المقالات</a>
      <a href="/Th-tech/category/" class="nav-link">التصنيفات</a>
      <a href="/Th-tech/contact/" class="nav-link">اتصل بنا</a>
    </nav>

    <div class="header-tools">
      <button id="search-btn" class="tool-btn">🔍</button>
      <button id="theme-btn" class="tool-btn">🌙</button>
      <button id="menu-btn" class="tool-btn hamburger">☰</button>
    </div>
  </div>

  <div id="search-panel" class="search-panel">
    <div class="search-container">
      <input type="text" placeholder="ابحث عن مقال..." class="search-input" />
      <button id="close-search" class="close-btn">✕</button>
    </div>
  </div>

  <nav id="mobile-menu" class="mobile-menu">
    <a href="/Th-tech/" class="mobile-link">🏠 الرئيسية</a>
    <a href="/Th-tech/articles/" class="mobile-link">📚 المقالات</a>
    <a href="/Th-tech/category/" class="mobile-link">📂 التصنيفات</a>
    <a href="/Th-tech/contact/" class="mobile-link">📧 اتصل بنا</a>
  </nav>
</header>

<style>
  /* 1. تعريف الألوان الافتراضية (الوضع النهاري) */
  :root {
    --bg: #f8fafc;
    --card: #ffffff;
    --text: #0f172a;
    --text-sec: #64748b;
    --primary: #0284c7;
    --border: rgba(2,132,199,0.2);
  }

  /* 2. تعريف ألوان الوضع الليلي */
  html.dark-mode {
    --bg: #0b1120;
    --card: #1e293b;
    --text: #f1f5f9;
    --text-sec: #94a3b8;
    --primary: #38bdf8;
    --border: rgba(56,189,248,0.2);
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  /* 3. تطبيق الخلفية على كامل الموقع لضمان عمل الوضع الليلي */
  body {
    background-color: var(--bg);
    color: var(--text);
    font-family: system-ui, sans-serif;
    transition: background-color 0.4s, color 0.4s;
  }
  
  .site-header {
    background: var(--card);
    border-bottom: 3px solid var(--border);
    position: sticky;
    top: 0;
    z-index: 1000;
    padding: 0.5rem 0;
  }
  
  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 2rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 140px; /* ارتفاع مناسب */
  }
  
  /* 4. ضبط حجم الشعار والمسافة */
  .logo {
    flex-shrink: 0;
    margin-left: 20px; /* مسافة تفصل الشعار عن الأزرار */
  }
  .logo-img {
    height: 100px; /* حجم متوازن */
    width: auto;
    display: block;
    filter: drop-shadow(0 4px 10px rgba(2,132,199,0.4));
  }
  
  .nav-links { display: flex; gap: 2rem; }
  .nav-link {
    color: var(--text);
    text-decoration: none;
    font-weight: 700;
    font-size: 1.1rem;
    transition: color 0.3s;
  }
  .nav-link:hover { color: var(--primary); }
  
  .header-tools { display: flex; gap: 1rem; }
  .tool-btn {
    background: var(--primary);
    border: 2px solid var(--border);
    color: white;
    width: 55px;
    height: 55px;
    border-radius: 14px;
    cursor: pointer;
    font-size: 1.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s;
  }
  .tool-btn:hover { transform: scale(1.1); }
  .hamburger { display: none; }
  
  .search-panel, .mobile-menu { display: none; }
  .search-panel.active, .mobile-menu.active { display: block; }
  
  .search-panel {
    position: absolute; top: 140px; left: 0; right: 0;
    background: var(--card); padding: 2rem;
    border-bottom: 2px solid var(--border); z-index: 999;
  }
  .search-container { max-width: 700px; margin: 0 auto; display: flex; gap: 1rem; }
  .search-input {
    flex: 1; padding: 1.2rem; border-radius: 12px;
    border: 2px solid var(--border); background: var(--bg); color: var(--text);
  }
  .close-btn { background: #ef4444; color: white; border: none; padding: 0 2rem; border-radius: 12px; cursor: pointer; font-weight: bold; }
  
  .mobile-menu {
    position: absolute; top: 140px; left: 0; right: 0;
    background: var(--card); padding: 2rem;
    border-bottom: 2px solid var(--border);
    display: none; flex-direction: column; z-index: 999;
  }
  .mobile-menu.active { display: flex; }
  .mobile-link {
    color: var(--text); text-decoration: none; padding: 1.5rem;
    border-bottom: 1px solid var(--border); font-weight: 700; font-size: 1.1rem;
  }
  .mobile-link:last-child { border-bottom: none; }

  @media (max-width: 768px) {
    .nav-links { display: none; }
    .hamburger { display: flex; }
    .logo-img { height: 70px; } /* تصغير الشعار للهاتف */
    .logo { margin-left: 10px; }
    .container { height: 100px; padding: 0 1.5rem; }
    .tool-btn { width: 50px; height: 50px; font-size: 1.3rem; }
    .search-panel, .mobile-menu { top: 100px; }
  }
</style>

<script is:inline>
  // دالة تبديل الوضع الليلي
  function toggleTheme() {
    const html = document.documentElement;
    const btn = document.getElementById('theme-btn');
    
    // التبديل بين إضافة وحذف الكلاس
    html.classList.toggle('dark-mode');
    
    // حفظ الاختيار وتغيير الأيقونة
    if (html.classList.contains('dark-mode')) {
      localStorage.setItem('theme', 'dark');
      btn.textContent = '☀️';
    } else {
      localStorage.setItem('theme', 'light');
      btn.textContent = '';
    }
  }

  // عند تحميل الصفحة
  document.addEventListener('DOMContentLoaded', () => {
    const btn = document.getElementById('theme-btn');
    const html = document.documentElement;
    const savedTheme = localStorage.getItem('theme');

    // استعادة الوضع المحفوظ
    if (savedTheme === 'dark') {
      html.classList.add('dark-mode');
      btn.textContent = '️';
    } else {
      html.classList.remove('dark-mode');
      btn.textContent = '';
    }

    // تفعيل الزر
    if (btn) {
      btn.addEventListener('click', toggleTheme);
    }

    // تفعيل قائمة الهاتف
    const menuBtn = document.getElementById('menu-btn');
    const mobileMenu = document.getElementById('mobile-menu');
    if (menuBtn && mobileMenu) {
      menuBtn.addEventListener('click', () => {
        mobileMenu.classList.toggle('active');
        menuBtn.textContent = mobileMenu.classList.contains('active') ? '✕' : '☰';
      });
    }

    // تفعيل البحث
    const searchBtn = document.getElementById('search-btn');
    const searchPanel = document.getElementById('search-panel');
    const closeSearch = document.getElementById('close-search');
    if (searchBtn && searchPanel) {
      searchBtn.addEventListener('click', () => searchPanel.classList.add('active'));
      if (closeSearch) closeSearch.addEventListener('click', () => searchPanel.classList.remove('active'));
    }
  });
</script>
