<footer class="footer">
  <div class="container">
    <div class="content">
      <div class="brand">
        <h3>Th.Tech</h3>
        <p>منصة عربية متخصصة في التقنية والذكاء الاصطناعي.</p>
      </div>
      <div class="links">
        <a href="/">الرئيسية</a>
        <a href="/articles/all">المقالات</a>
        <a href="#">سياسة الخصوصية</a>
      </div>
      <div class="socials">
        <a href="#" aria-label="Twitter">𝕏</a>
        <a href="#" aria-label="GitHub">GH</a>
      </div>
    </div>
    <div class="copyright">
      &copy; {new Date().getFullYear()} Th-Tech. جميع الحقوق محفوظة.
    </div>
  </div>
</footer>

<style>
  .footer { background: var(--surface); border-top: 1px solid var(--border); padding: 4rem 0 2rem; margin-top: 4rem; }
  .container { max-width: 1200px; margin: 0 auto; padding: 0 1.5rem; }
  .content { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 2rem; margin-bottom: 3rem; }
  .brand h3 { font-size: 1.5rem; margin-bottom: 0.5rem; color: var(--primary); }
  .brand p { color: var(--muted); font-size: 0.9rem; }
  .links { display: flex; flex-direction: column; gap: 0.8rem; }
  .links a { color: var(--muted); transition: 0.2s; }
  .links a:hover { color: var(--primary); }
  .socials { display: flex; gap: 1rem; }
  .socials a { color: var(--muted); font-weight: 700; font-size: 1.2rem; }
  .socials a:hover { color: var(--accent); }
  .copyright { text-align: center; color: var(--muted); font-size: 0.8rem; border-top: 1px solid var(--border); padding-top: 2rem; }
</style>
